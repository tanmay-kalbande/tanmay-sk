{
  "slug": "build-rest-apis-with-nodejs-and-express-beginner",
  "title": "From Zero to Hero: Building REST APIs with Node.js and Express",
  "goal": "Build REST APIs with Node.js and Express",
  "category": "programming",
  "tags": [
    "nodejs",
    "backend"
  ],
  "language": "en",
  "complexity": "beginner",
  "wordCount": 8515,
  "moduleCount": 8,
  "readingTimeMins": 35,
  "metaDescription": "8,515-word guide: Build REST APIs with Node.js and Express. Free to read on Pustakam.",
  "generatedAt": "2026-07-14T02:34:43.463Z",
  "roadmap": {
    "title": "From Zero to Hero: Building REST APIs with Node.js and Express",
    "modules": [
      {
        "title": "Module 1: Introduction to Node.js and NPM",
        "description": "Learn what Node.js is, how to install it, and use NPM to manage packages.",
        "objectives": [
          "Understand Node.js and its runtime environment",
          "Install Node.js and NPM on your system",
          "Create and manage a Node.js project using NPM"
        ]
      },
      {
        "title": "Module 2: Getting Started with Express.js",
        "description": "Set up a basic Express server and understand routing fundamentals.",
        "objectives": [
          "Install and set up Express.js in a Node.js project",
          "Create a basic server with Express",
          "Define and handle routes using HTTP methods"
        ]
      },
      {
        "title": "Module 3: Working with Middleware",
        "description": "Explore middleware in Express and how it processes requests.",
        "objectives": [
          "Understand the concept and role of middleware in Express",
          "Use built-in middleware like express.json() and express.urlencoded()",
          "Create custom middleware functions"
        ]
      },
      {
        "title": "Module 4: Handling Requests and Responses",
        "description": "Learn to process incoming requests and send appropriate responses.",
        "objectives": [
          "Extract data from request objects (params, query, body)",
          "Send responses with different status codes",
          "Handle errors and send meaningful error responses"
        ]
      },
      {
        "title": "Module 5: Connecting to a Database",
        "description": "Integrate a database (MongoDB) with your Express API using Mongoose.",
        "objectives": [
          "Set up MongoDB and connect it to your Node.js application",
          "Define and interact with data models using Mongoose",
          "Perform CRUD operations with Mongoose"
        ]
      },
      {
        "title": "Module 6: Building a RESTful API",
        "description": "Design and implement a complete RESTful API with Express and MongoDB.",
        "objectives": [
          "Understand REST principles and API design best practices",
          "Create API endpoints for CRUD operations",
          "Implement proper HTTP status codes and response formats"
        ]
      },
      {
        "title": "Module 7: Authentication and Authorization",
        "description": "Secure your API using JWT for authentication and role-based access.",
        "objectives": [
          "Implement user authentication with JWT",
          "Use middleware to protect routes",
          "Manage user roles and permissions"
        ]
      },
      {
        "title": "Module 8: Testing and Deployment",
        "description": "Test your API and deploy it to a cloud platform like Render or Railway.",
        "objectives": [
          "Write unit and integration tests using tools like Jest or Postman",
          "Prepare your API for production",
          "Deploy your API to a cloud service and monitor its performance"
        ]
      }
    ],
    "difficultyLevel": "beginner"
  },
  "modules": [
    {
      "title": "Module 1: Introduction to Node.js and NPM",
      "content": "## **Chapter 1: Introduction to Node.js and NPM**\n\nWelcome to the first module of *Build REST APIs with Node.js and Express*! In this chapter, we’ll lay the foundation by understanding Node.js, its runtime environment, and how to set up your development environment using Node Package Manager (NPM). By the end, you’ll be ready to create your first Node.js project and manage dependencies efficiently.\n\n---\n\n## **What is Node.js?**\n\nNode.js is an open-source, cross-platform **JavaScript runtime environment** that allows you to execute JavaScript code outside a web browser. Unlike traditional JavaScript, which runs in the browser, Node.js runs on the server, enabling **server-side scripting** and the development of scalable network applications.\n\n### **Key Features of Node.js**\n1. **Event-Driven & Non-Blocking I/O**: Node.js uses an event loop to handle asynchronous operations efficiently, making it ideal for I/O-heavy applications (e.g., APIs, real-time chat apps).\n2. **Single-Threaded with Event Loop**: Despite being single-threaded, Node.js leverages an event-driven architecture to handle multiple requests concurrently.\n3. **JavaScript Everywhere**: With Node.js, you can use JavaScript for both **frontend** and **backend** development, reducing context-switching.\n4. **NPM (Node Package Manager)**: A vast ecosystem of reusable code packages (libraries/modules) that accelerates development.\n\n### **Use Cases for Node.js**\n- **RESTful APIs** (like the ones we’ll build in this course)\n- **Real-time applications** (e.g., chat apps, gaming servers)\n- **Microservices architecture**\n- **Serverless functions** (AWS Lambda, Firebase Functions)\n- **Command-line tools** (e.g., Babel, Webpack)\n\n**Example 1: Simple HTTP Server in Node.js**\nLet’s create a basic HTTP server to understand how Node.js handles requests:\n\n```javascript\n// server.js\nconst http = require('http');\n\nconst server = http.createServer((req, res) => {\n  res.writeHead(200, { 'Content-Type': 'text/plain' });\n  res.end('Hello from Node.js!');\n});\n\nserver.listen(3000, () => {\n  console.log('Server running on http://localhost:3000');\n});\n```\n**How it works:**\n1. We import Node’s built-in `http` module.\n2. We create a server that listens on port `3000`.\n3. When a request is made, the server responds with `\"Hello from Node.js!\"`.\n\nRun this code using:\n```bash\nnode server.js\n```\nVisit `http://localhost:3000` in your browser to see the response.\n\n---\n\n## **Installing Node.js and NPM**\n\n### **Step 1: Downloading Node.js**\nNode.js includes **NPM** (Node Package Manager) by default, so installing Node.js automatically installs NPM.\n\n1. **Download Node.js** from the [official website](https://nodejs.org/).\n   - Choose the **LTS (Long-Term Support)** version for stability.\n   - The installer includes NPM.\n\n2. **Run the installer** and follow the steps (default settings are fine).\n\n3. **Verify Installation**\n   Open a terminal (Command Prompt, PowerShell, or Terminal) and run:\n   ```bash\n   node -v\n   npm -v\n   ```\n   You should see the installed versions, e.g.:\n   ```\n   v20.12.2  # Node.js version\n   10.5.0    # NPM version\n   ```\n\n### **Step 2: Updating Node.js and NPM**\nTo update Node.js and NPM later, you can:\n- **Reinstall Node.js** from the website.\n- Use **NPM** to update itself:\n  ```bash\n  npm install -g npm@latest\n  ```\n- Use a version manager like **nvm** (Node Version Manager) for more control (recommended for advanced users).\n\n**Example 2: Using NPM to Install a Package**\nLet’s install `express`, the web framework we’ll use later:\n```bash\nnpm install express\n```\nThis downloads `express` and its dependencies into a `node_modules` folder.\n\n---\n\n## **Creating and Managing a Node.js Project with NPM**\n\nEvery Node.js project should have a `package.json` file, which acts as a **manifest** for your project. It stores metadata (name, version, dependencies) and scripts.\n\n### **Step 1: Initialize a Node.js Project**\n1. Create a new folder for your project:\n   ```bash\n   mkdir my-first-node-app\n   cd my-first-node-app\n   ```\n2. Initialize the project:\n   ```bash\n   npm init -y\n   ```\n   The `-y` flag skips the interactive questionnaire (default values are used).\n\n   This creates a `package.json` file:\n   ```json\n   {\n     \"name\": \"my-first-node-app\",\n     \"version\": \"1.0.0\",\n     \"description\": \"\",\n     \"main\": \"index.js\",\n     \"scripts\": {\n       \"test\": \"echo \\\"Error: no test specified\\\" && exit 1\"\n     },\n     \"keywords\": [],\n     \"author\": \"\",\n     \"license\": \"ISC\"\n   }\n   ```\n\n### **Step 2: Adding Dependencies**\nTo add a package (e.g., `express`):\n```bash\nnpm install express\n```\nThis:\n1. Downloads `express` into `node_modules`.\n2. Adds `express` to `dependencies` in `package.json`:\n   ```json\n   \"dependencies\": {\n     \"express\": \"^4.18.2\"\n   }\n   ```\n\n### **Step 3: Running Scripts**\nYou can define custom scripts in `package.json` under `\"scripts\"`. For example:\n```json\n\"scripts\": {\n  \"start\": \"node server.js\",\n  \"dev\": \"nodemon server.js\"\n}\n```\n- Run `npm start` to start your app.\n- Run `npm run dev` if you have `nodemon` installed (a tool that auto-restarts the server on file changes).\n\n### **Step 4: Managing `node_modules`**\n- **Install all dependencies** listed in `package.json`:\n  ```bash\n  npm install\n  ```\n- **Remove a package**:\n  ```bash\n  npm uninstall express\n  ```\n- **Save a package as a `devDependency`** (used only in development):\n  ```bash\n  npm install nodemon --save-dev\n  ```\n\n---\n\n## **Understanding the Node.js Runtime Environment**\n\nNode.js runs on the **V8 JavaScript engine** (the same engine used in Chrome), which compiles JavaScript to native machine code for fast execution. Key components of Node.js include:\n\n1. **Event Loop**: Handles asynchronous operations (e.g., file I/O, network requests) without blocking the main thread.\n2. **Modules**: Node.js uses the `require()` function to import built-in modules (e.g., `http`, `fs`) or third-party packages.\n3. **Process Object**: Provides information about the running Node.js process (e.g., `process.env` for environment variables).\n4. **Buffer Class**: Handles binary data (important for file operations and network streams).\n\n---\n\n## **Summary**\nIn this chapter, we:\n1. Learned what Node.js is and why it’s powerful.\n2. Installed Node.js and verified the installation.\n3. Created a Node.js project and managed dependencies with NPM.\n\nYou now have a working Node.js environment ready for building APIs!\n\n---\n\n## **Key Takeaways**\n- **Node.js** is a JavaScript runtime for server-side development, enabling scalable, event-driven applications.\n- **NPM** is the default package manager for Node.js, allowing you to install, update, and manage libraries (packages).\n- A **Node.js project** is defined by its `package.json` file, which tracks dependencies and scripts.",
      "wordCount": 942
    },
    {
      "title": "Module 2: Getting Started with Express.js",
      "content": "## Module 2: Getting Started with Express.js\n\nExpress.js is a minimal and flexible web application framework for Node.js that provides a robust set of features to build web and mobile applications. In this module, you'll learn how to set up a basic Express server, understand routing fundamentals, and handle HTTP methods effectively. By the end, you'll have a solid foundation to start building RESTful APIs with Express.\n\n---\n\n### **Setting Up Express.js in a Node.js Project**\n\nBefore diving into Express, ensure you have Node.js installed on your system. You can verify this by running:\n\n```bash\nnode -v\nnpm -v\n```\n\nIf Node.js is not installed, download it from [nodejs.org](https://nodejs.org/).\n\n#### **Step 1: Initialize a Node.js Project**\nCreate a new directory for your project and initialize it with npm:\n\n```bash\nmkdir express-api\ncd express-api\nnpm init -y\n```\n\nThis creates a `package.json` file with default settings.\n\n#### **Step 2: Install Express.js**\nInstall Express as a dependency in your project:\n\n```bash\nnpm install express\n```\n\nThis adds Express to your `node_modules` and updates the `package.json` file.\n\n#### **Step 3: Create a Basic Server**\nCreate a file named `app.js` (or `index.js`) in your project root. This will be the entry point of your application. Add the following code to set up a basic Express server:\n\n```javascript\n// Import the express module\nconst express = require('express');\n\n// Create an instance of an Express application\nconst app = express();\n\n// Define the port number\nconst PORT = 3000;\n\n// Define a route for the root path ('/')\napp.get('/', (req, res) => {\n    res.send('Welcome to the Home Page!');\n});\n\n// Start the server\napp.listen(PORT, () => {\n    console.log(`Server is running on http://localhost:${PORT}`);\n});\n```\n\n#### **Step 4: Run the Server**\nExecute your server using Node.js:\n\n```bash\nnode app.js\n```\n\nOpen your web browser and navigate to `http://localhost:3000`. You should see the message \"Welcome to the Home Page!\" displayed.\n\n#### **Step 5: Enhance the Server with Middleware**\nExpress.js middleware functions have access to the request (`req`), response (`res`), and the next middleware function in the cycle. Middleware can modify the request and response objects, end the request-response cycle, or call the next middleware function.\n\nA common middleware is `express.json()`, which parses incoming JSON requests:\n\n```javascript\n// Add JSON parsing middleware\napp.use(express.json());\n\n// Example route that accepts JSON data\napp.post('/api/data', (req, res) => {\n    const receivedData = req.body;\n    res.json({\n        message: 'Data received successfully!',\n        data: receivedData\n    });\n});\n```\n\nNow, if you send a POST request to `http://localhost:3000/api/data` with a JSON payload, the server will respond with the received data.\n\n---\n\n### **Understanding Routing Fundamentals**\n\nRouting refers to how an application’s endpoints (URIs) respond to client requests. In Express, routes are defined using HTTP methods (e.g., `GET`, `POST`, `PUT`, `DELETE`) and path patterns.\n\n#### **Basic Route Structure**\nA basic route is defined as:\n\n```javascript\napp.METHOD(PATH, HANDLER);\n```\n\n- `METHOD`: The HTTP method (e.g., `get`, `post`, `put`, `delete`).\n- `PATH`: The route path (e.g., `'/'`, `/users`, `/products/:id`).\n- `HANDLER`: The function executed when the route is matched.\n\n#### **Route Parameters**\nRoute parameters are named URL segments used to capture values from the URL. For example, if you have a route like `/users/:userId`, `:userId` is a route parameter that can be accessed via `req.params`.\n\n```javascript\n// Example route with a parameter\napp.get('/users/:userId', (req, res) => {\n    const userId = req.params.userId;\n    res.send(`User ID: ${userId}`);\n});\n```\n\nIf you visit `http://localhost:3000/users/123`, the server will respond with \"User ID: 123\".\n\n#### **Handling Different HTTP Methods**\nExpress supports all HTTP methods. Here are examples for common methods:\n\n##### **GET Request**\nRetrieve data from the server.\n\n```javascript\n// Example: Fetch a list of products\napp.get('/products', (req, res) => {\n    const products = [\n        { id: 1, name: 'Laptop' },\n        { id: 2, name: 'Phone' },\n        { id: 3, name: 'Tablet' }\n    ];\n    res.json(products);\n});\n```\n\n##### **POST Request**\nSubmit data to the server to create a new resource.\n\n```javascript\n// Example: Create a new user\napp.post('/users', (req, res) => {\n    const newUser = {\n        id: 4,\n        name: req.body.name,\n        email: req.body.email\n    };\n    // In a real app, you'd save this to a database\n    res.status(201).json(newUser);\n});\n```\n\n##### **PUT/PATCH Request**\nUpdate an existing resource.\n\n```javascript\n// Example: Update a user's email\napp.put('/users/:userId', (req, res) => {\n    const userId = req.params.userId;\n    const updatedUser = {\n        id: userId,\n        name: req.body.name,\n        email: req.body.email\n    };\n    res.json(updatedUser);\n});\n```\n\n##### **DELETE Request**\nRemove a resource from the server.\n\n```javascript\n// Example: Delete a user\napp.delete('/users/:userId', (req, res) => {\n    const userId = req.params.userId;\n    res.status(204).send(); // No content, successful deletion\n});\n```\n\n---\n\n### **Practical Example: Building a Simple REST API**\n\nLet's put everything together by creating a simple REST API for managing a list of books.\n\n#### **Step 1: Define the Book Data**\nCreate an array to store book data temporarily.\n\n```javascript\nlet books = [\n    { id: 1, title: 'The Great Gatsby', author: 'F. Scott Fitzgerald' },\n    { id: 2, title: '1984', author: 'George Orwell' },\n    { id: 3, title: 'To Kill a Mockingbird', author: 'Harper Lee' }\n];\n```\n\n#### **Step 2: Define Routes**\nAdd the following routes to your `app.js`:\n\n```javascript\n// Middleware to parse JSON bodies\napp.use(express.json());\n\n// GET all books\napp.get('/books', (req, res) => {\n    res.json(books);\n});\n\n// GET a single book by ID\napp.get('/books/:id', (req, res) => {\n    const bookId = parseInt(req.params.id);\n    const book = books.find(b => b.id === bookId);\n\n    if (book) {\n        res.json(book);\n    } else {\n        res.status(404).json({ message: 'Book not found' });\n    }\n});\n\n// POST a new book\napp.post('/books', (req, res) => {\n    const newBook = {\n        id: books.length + 1,\n        title: req.body.title,\n        author: req.body.author\n    };\n    books.push(newBook);\n    res.status(201).json(newBook);\n});\n\n// PUT (update) a book\napp.put('/books/:id', (req, res) => {\n    const bookId = parseInt(req.params.id);\n    const bookIndex = books.findIndex(b => b.id === bookId);\n\n    if (bookIndex !== -1) {\n        const updatedBook = {\n            id: bookId,\n            title: req.body.title,\n            author: req.body.author\n        };\n        books[bookIndex] = updatedBook;\n        res.json(updatedBook);\n    } else {\n        res.status(404).json({ message: 'Book not found' });\n    }\n});\n\n// DELETE a book\napp.delete('/books/:id', (req, res) => {\n    const bookId = parseInt(req.params.id);\n    const bookIndex = books.findIndex(b => b.id === bookId);\n\n    if (bookIndex !== -1) {\n        books.splice(bookIndex, 1);\n        res.status(204).send();\n    } else {\n        res.status(404).json({ message: 'Book not found' });\n    }\n});\n```\n\n#### **Step 3: Test the API**\nYou can test the API using tools like [Postman](https://www.postman.com/), [Insomnia](https://insomnia.rest/), or even the command line with `curl`.\n\nFor example, to get all books using `curl`:\n\n```bash\ncurl http://localhost:3000/books\n```\n\nTo create a new book:\n\n```bash\ncurl -X POST http://localhost:3000/books \\\n-H \"Content-Type: application/json\" \\\n-d '{\"title\": \"The Hobbit\", \"author\": \"J.R.R. Tolkien\"}'\n```\n\n---\n\n## Key Takeaways\n\n- **Express.js Simplifies Server Creation**: With Express, you can set up a web server in just a few lines of code using methods like `app.get()`, `app.post()`, etc.\n- **Routing is Central to Express**: Routes in Express are defined using HTTP methods and path patterns, making it easy to handle different",
      "wordCount": 1130
    },
    {
      "title": "Module 3: Working with Middleware",
      "content": "## Module 3: Working with Middleware\n\nExpress.js is a minimal and flexible Node.js web application framework that provides a robust set of features for web and mobile applications. One of the core concepts that make Express powerful is **middleware**. Middleware functions are functions that have access to the request object (`req`), the response object (`res`), and the next middleware function in the application’s request-response cycle. In this chapter, we’ll explore what middleware is, how it works in Express, and how to use both built-in and custom middleware effectively.\n\n---\n\n## Understanding Middleware in Express\n\n### What Is Middleware?\n\nMiddleware is software that sits between an operating system and the applications running on it, acting as a translator or filter. In the context of Express, middleware functions are software that process incoming requests before they reach the final route handler. They can:\n\n- Execute any code.\n- Make changes to the request and response objects.\n- End the request-response cycle (e.g., by sending a response).\n- Call the next middleware function in the stack.\n\nThink of middleware as a series of checkpoints in a pipeline. Each middleware function has the chance to inspect, modify, or reject requests before they reach the final destination.\n\n### The Request-Response Cycle with Middleware\n\nWhen a client sends a request to your Express app:\n\n1. The request enters the middleware pipeline.\n2. Each middleware function can process the request or pass it to the next function.\n3. If a middleware ends the response (e.g., by sending a response), the cycle stops.\n4. If all middleware passes the request, it reaches the final route handler.\n\nHere’s a simple example:\n\n```javascript\nconst express = require('express');\nconst app = express();\n\n// Middleware 1: Logs the time of the request\napp.use((req, res, next) => {\n  console.log(`Time: ${Date.now()}`);\n  next(); // Pass control to the next middleware\n});\n\n// Middleware 2: Logs the HTTP method and URL\napp.use((req, res, next) => {\n  console.log(`${req.method} request for ${req.url}`);\n  next();\n});\n\n// Route handler\napp.get('/', (req, res) => {\n  res.send('Hello World!');\n});\n\napp.listen(3000, () => {\n  console.log('Server running on port 3000');\n});\n```\n\nWhen you run this app and visit `http://localhost:3000/`, you’ll see two log lines in your console:\n```\nTime: 1712345678901\nGET request for /\nHello World!\n```\n\nThis shows how two middleware functions processed the request before it reached the final route handler.\n\n---\n\n## Using Built-in Middleware\n\nExpress comes with several built-in middleware functions that handle common tasks like parsing request bodies, serving static files, and handling CORS. Let's look at the most commonly used ones.\n\n---\n\n### 1. `express.json()` – Parsing JSON Requests\n\nWhen a client sends a JSON payload in the body of a POST or PUT request, you need to parse it into a JavaScript object before accessing its data. The `express.json()` middleware does exactly that.\n\n**How to Use:**\n\n```javascript\nconst express = require('express');\nconst app = express();\n\n// Use the json middleware to parse JSON request bodies\napp.use(express.json());\n\napp.post('/users', (req, res) => {\n  const user = req.body; // req.body is now a parsed JavaScript object\n  console.log('New user:', user);\n  res.status(201).json({ message: 'User created', user });\n});\n\napp.listen(3000, () => {\n  console.log('Server running on port 3000');\n});\n```\n\n**Example Request:**\n\n```bash\ncurl -X POST http://localhost:3000/users \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"name\": \"Alice\", \"email\": \"alice@example.com\"}'\n```\n\n**Output:**\n```\nNew user: { name: 'Alice', email: 'alice@example.com' }\n```\n\nThis middleware is essential for REST APIs that accept JSON input.\n\n---\n\n### 2. `express.urlencoded()` – Parsing URL-encoded Data\n\nThis middleware parses incoming requests with URL-encoded payloads (e.g., from HTML forms). It converts the data into a JavaScript object and attaches it to `req.body`.\n\n```javascript\napp.use(express.urlencoded({ extended: true }));\n\napp.post('/login', (req, res) => {\n  const { username, password } = req.body;\n  console.log(`Login attempt: ${username}`);\n  res.send(`Welcome, ${username}!`);\n});\n```\n\n> 🔍 **Note:** The `{ extended: true }` option allows parsing of rich objects and arrays (using the `qs` library), while `{ extended: false }` uses the simpler `querystring` library.\n\n---\n\n## Creating Custom Middleware Functions\n\nWhile built-in middleware covers common use cases, real-world applications often require custom logic. You can create your own middleware functions that perform specific tasks.\n\n### Structure of a Custom Middleware\n\nA custom middleware function takes three parameters:\n\n```javascript\n(req, res, next) => { ... }\n```\n\n- `req`: The request object.\n- `res`: The response object.\n- `next`: A function to pass control to the next middleware.\n\nIf you don’t call `next()`, the request will hang unless you send a response.\n\n---\n\n### Example 1: Authentication Middleware\n\nLet’s create a simple authentication middleware that checks for a valid API key.\n\n```javascript\n// Middleware to check API key\nconst authenticate = (req, res, next) => {\n  const apiKey = req.headers['x-api-key'];\n\n  if (apiKey && apiKey === 'SECRET123') {\n    // User is authenticated\n    req.user = { id: 1, name: 'Admin' };\n    next(); // Proceed to the next middleware\n  } else {\n    // Not authorized\n    res.status(401).json({ error: 'Unauthorized: Invalid API key' });\n  }\n};\n\n// Use the middleware on protected routes\napp.get('/dashboard', authenticate, (req, res) => {\n  res.json({ message: `Welcome to your dashboard, ${req.user.name}!` });\n});\n```\n\n**Test with:**\n\n```bash\ncurl http://localhost:3000/dashboard\n# Response: 401 Unauthorized\n\ncurl -H \"x-api-key: SECRET123\" http://localhost:3000/dashboard\n# Response: { \"message\": \"Welcome to your dashboard, Admin!\" }\n```\n\nThis is a simple example, but it demonstrates how middleware can enforce security rules.\n\n---\n\n### Example 2: Request Logging Middleware\n\nAnother common use case is logging requests for debugging or monitoring.\n\n```javascript\nconst requestLogger = (req, res, next) => {\n  const timestamp = new Date().toISOString();\n  const method = req.method;\n  const url = req.url;\n\n  console.log(`[${timestamp}] ${method} ${url}`);\n\n  // Optionally log IP and user agent\n  console.log(`IP: ${req.ip}, User-Agent: ${req.get('User-Agent')}`);\n\n  next(); // Always call next() unless you want to block the request\n};\n\napp.use(requestLogger); // Apply to all routes\n```\n\nThis middleware logs every incoming request with a timestamp, method, URL, IP, and user agent, which is helpful for debugging and analytics.\n\n---\n\n## Advanced Middleware Concepts\n\n### Order Matters\n\nMiddleware functions are executed in the order they are added. This is crucial—if you place authentication middleware after a route, it won’t protect that route.\n\n```javascript\napp.use(express.json()); // Parse JSON first\n\n// Authentication middleware\napp.use(authenticate); // Then check auth\n\n// Then define routes\napp.get('/profile', (req, res) => {\n  res.json({ user: req.user });\n});\n```\n\nIf you reverse the order, users won’t be authenticated when they reach `/profile`.\n\n---\n\n### Using Middleware on Specific Routes\n\nYou can apply middleware to specific routes using `.use()` or directly in route handlers.\n\n```javascript\n// Middleware only for /admin routes\napp.use('/admin', adminAuthMiddleware);\n\n// Or in a route\napp.get('/account', authMiddleware, (req, res) => {\n  // Only authenticated users reach here\n});\n```\n\n---\n\n### Error-handling Middleware\n\nExpress supports special middleware functions with **four parameters** to handle errors:\n\n```javascript\n(app, err, req, res, next) => { ... }\n```\n\n```javascript\n// Error-handling middleware\napp.use((err, req, res, next) => {\n  console.error(err.stack);\n  res.status(500).json({ error: 'Something went wrong!' });\n});\n```\n\n> ⚠️ Error middleware must be defined **after all other middleware and routes**.\n\n---\n\n## Best Practices\n\n1. **Keep Middleware Small and Focused**: Each middleware should do one thing well.\n2. **Use Middleware for Cross-cutting Concerns**: Logging, authentication, CORS, etc.\n3. **Order Matters**: Place middleware in the correct sequence.\n4. **Avoid Blocking the Event Loop**: Don’t perform CPU-intensive tasks in middleware.\n5. **Test Middleware Independently**: Write unit tests for custom middleware.\n\n---\n\n## Real-World Example: Building a RESTful API with Middleware\n\nLet’s build a small REST API for a blog with the following features:\n\n- Parse JSON and URL-encoded data.\n- Log all requests.\n- Validate API keys.\n- Handle errors globally.\n\n```javascript\nconst express = require('express');\nconst app =",
      "wordCount": 1268
    },
    {
      "title": "Module 4: Handling Requests and Responses",
      "content": "## Handling Requests and Responses in Express\n\nIn the previous chapters, we set up a basic Express server and learned how to handle routes. Now, we'll dive deeper into processing incoming requests and sending appropriate responses. This is a core skill for building REST APIs, as it determines how your application interacts with clients.\n\n### Understanding the Request Object\n\nEvery time a client makes a request to your server, Express creates a `Request` object containing all the information sent by the client. This object includes:\n\n- Route parameters (e.g., `/users/:id`)\n- Query strings (e.g., `?name=John&age=30`)\n- Request body (e.g., JSON data from POST requests)\n- Headers (e.g., `Content-Type`, `Authorization`)\n- HTTP method (GET, POST, PUT, DELETE, etc.)\n\nLet's see how to extract and use each of these components.\n\n---\n\n### Extracting Route Parameters\n\nRoute parameters are dynamic segments in your URL that capture values from the request path. For example, in the route `/products/:id`, `:id` is a parameter that matches whatever comes after `/products/` (e.g., `/products/123`).\n\n```javascript\nconst express = require('express');\nconst app = express();\n\n// Define a route with a parameter\napp.get('/products/:id', (req, res) => {\n  const productId = req.params.id; // Extract the 'id' parameter\n  res.send(`You requested product with ID: ${productId}`);\n});\n\napp.listen(3000, () => {\n  console.log('Server running on http://localhost:3000');\n});\n```\n\nIn this example:\n- A GET request to `/products/42` will respond with `\"You requested product with ID: 42\"`.\n- The value `42` is extracted from the URL and stored in `req.params.id`.\n\nRoute parameters are especially useful for retrieving specific resources, like fetching a user by their ID.\n\n---\n\n### Working with Query Strings\n\nQuery strings are key-value pairs appended to the URL after a `?` (e.g., `/search?q=express&sort=desc`). They are commonly used for filtering, sorting, or pagination in APIs.\n\n```javascript\napp.get('/search', (req, res) => {\n  const query = req.query; // Extract the query object\n  res.json({\n    query: query.q,\n    sort: query.sort,\n    message: `Searching for \"${query.q}\" with sort order \"${query.sort}\"`\n  });\n});\n```\n\nA request to `/search?q=rest&sort=asc` would respond with:\n\n```json\n{\n  \"query\": \"rest\",\n  \"sort\": \"asc\",\n  \"message\": \"Searching for \\\"rest\\\" with sort order \\\"asc\\\"\"\n}\n```\n\nTo handle optional query parameters, always check if they exist before using them:\n\n```javascript\napp.get('/users', (req, res) => {\n  const limit = req.query.limit || 10; // Default to 10 if not provided\n  res.send(`Returning up to ${limit} users`);\n});\n```\n\n---\n\n### Accessing the Request Body\n\nTo handle data sent in the request body (e.g., from POST or PUT requests), you need to use middleware that parses the body. Express has built-in middleware for JSON and URL-encoded data.\n\n#### Parsing JSON Data\n\nFor APIs that send JSON data (common in REST APIs), use `express.json()`:\n\n```javascript\napp.use(express.json()); // Middleware to parse JSON bodies\n\napp.post('/users', (req, res) => {\n  const user = req.body; // Access the parsed JSON data\n  console.log('New user:', user);\n  res.status(201).send(`User ${user.name} created!`);\n});\n```\n\nA POST request with the body:\n\n```json\n{\n  \"name\": \"Alice\",\n  \"email\": \"alice@example.com\"\n}\n```\n\nWould respond with:\n\n```\nUser Alice created!\n```\n\n> ⚠️ **Important**: Always use `express.json()` (or equivalent middleware) before defining routes that expect a body, or the body will be `undefined`.\n\n#### Parsing URL-Encoded Data\n\nFor form submissions (e.g., `application/x-www-form-urlencoded`), use `express.urlencoded()`:\n\n```javascript\napp.use(express.urlencoded({ extended: true }));\n\napp.post('/login', (req, res) => {\n  const { username, password } = req.body;\n  // Validate credentials...\n  res.send(`Welcome, ${username}!`);\n});\n```\n\n---\n\n### Sending Responses\n\nExpress provides several methods to send responses to the client. The most common are:\n\n- `res.send()` – Sends a response of various types (string, object, buffer)\n- `res.json()` – Sends a JSON response\n- `res.status(code).send()` – Sets HTTP status code and sends response\n- `res.redirect()` – Redirects to another URL\n\n#### Example: Sending JSON Responses\n\n```javascript\napp.get('/api/user/1', (req, res) => {\n  const user = {\n    id: 1,\n    name: \"John Doe\",\n    email: \"john@example.com\"\n  };\n  res.json(user); // Automatically sets Content-Type: application/json\n});\n```\n\n#### Example: Using Status Codes\n\nAlways use appropriate HTTP status codes:\n\n```javascript\napp.post('/orders', (req, res) => {\n  const order = createOrder(req.body); // Assume this creates an order\n  res.status(201).json({\n    message: 'Order created',\n    orderId: order.id\n  });\n});\n```\n\nCommon status codes:\n- `200 OK` – Success\n- `201 Created` – Resource created\n- `400 Bad Request` – Invalid client input\n- `404 Not Found` – Resource doesn't exist\n- `500 Internal Server Error` – Server-side error\n\n---\n\n### Handling Errors and Sending Error Responses\n\nErrors are inevitable in any application. The key is to handle them gracefully and send meaningful feedback to the client.\n\n#### Basic Error Handling\n\n```javascript\napp.get('/divide', (req, res) => {\n  const a = parseInt(req.query.a);\n  const b = parseInt(req.query.b);\n\n  if (isNaN(a) || isNaN(b)) {\n    return res.status(400).json({\n      error: \"Invalid input: 'a' and 'b' must be numbers.\"\n    });\n  }\n\n  if (b === 0) {\n    return res.status(400).json({\n      error: \"Division by zero is not allowed.\"\n    });\n  }\n\n  const result = a / b;\n  res.json({ result });\n});\n```\n\nA request to `/divide?a=10&b=2` returns:\n```json\n{ \"result\": 5 }\n```\n\nA request to `/divide?a=10&b=0` returns:\n```json\n{ \"error\": \"Division by zero is not allowed.\" }\n```\n\n> ✅ **Tip**: Always `return` after sending a response in error handlers to stop further execution.\n\n---\n\n### Using Middleware for Consistent Error Handling\n\nFor a cleaner approach, define an error-handling middleware. Express recognizes error-handling middleware by its four parameters: `(err, req, res, next)`.\n\n```javascript\n// Error-handling middleware\napp.use((err, req, res, next) => {\n  console.error(err.stack);\n  res.status(500).json({\n    error: 'Something went wrong!',\n    message: process.env.NODE_ENV === 'development' ? err.message : undefined\n  });\n});\n\n// Example route that throws an error\napp.get('/risky', (req, res, next) => {\n  try {\n    throw new Error('Oops! Something failed.');\n  } catch (err) {\n    next(err); // Pass error to the error handler\n  }\n});\n```\n\nIn development, this might return:\n```json\n{\n  \"error\": \"Something went wrong!\",\n  \"message\": \"Oops! Something failed.\"\n}\n```\n\nIn production, sensitive details are hidden.\n\n---\n\n### Real-World Example: Building a Simple User API\n\nLet's put it all together in a small user management API.\n\n```javascript\nconst express = require('express');\nconst app = express();\n\n// Middleware\napp.use(express.json());\n\n// In-memory \"database\"\nlet users = [\n  { id: 1, name: 'Alice', email: 'alice@example.com' },\n  { id: 2, name: 'Bob', email: 'bob@example.com' }\n];\n\n// GET /users – List all users or filter by query\napp.get('/users', (req, res) => {\n  const name = req.query.name?.toLowerCase();\n  const filteredUsers = name\n    ? users.filter(u => u.name.toLowerCase().includes(name))\n    : users;\n\n  res.json(filteredUsers);\n});\n\n// GET /users/:id – Get a single user\napp.get('/users/:id', (req, res) => {\n  const userId = parseInt(req.params.id);\n  const user = users.find(u => u.id === userId);\n\n  if (!user) {\n    return res.status(404).json({ error: 'User not found' });\n  }\n\n  res.json(user);\n});\n\n// POST /users – Create a new user\napp.post('/users', (req, res) => {\n  const { name, email } = req.body;\n\n  if (!name || !email) {\n    return res.status(400).json({ error: 'Name and email are required' });\n  }\n\n  const newUser = {\n    id: users.length",
      "wordCount": 1120
    },
    {
      "title": "Module 5: Connecting to a Database",
      "content": "## **Chapter 5: Connecting to a Database – Integrating MongoDB with Mongoose**\n\nBuilding a REST API is only half the battle—what good is your API if it can’t store or retrieve data? In this chapter, we’ll connect your Express application to a **MongoDB** database using **Mongoose**, a popular Object Data Modeling (ODM) library for Node.js. By the end, you’ll know how to set up MongoDB, define data models, and perform CRUD operations with ease.\n\n---\n\n## **1. Setting Up MongoDB**\n\nMongoDB is a NoSQL database that stores data in flexible, JSON-like documents. Before we connect it to our API, we need to set it up.\n\n### **Option 1: Local MongoDB Installation**\n1. **Install MongoDB Community Server** from [https://www.mongodb.com/try/download/community](https://www.mongodb.com/try/download/community).\n2. **Start the MongoDB service**:\n   - On **Windows**, run `mongod` in the Command Prompt.\n   - On **macOS/Linux**, use:\n     ```bash\n     brew services start mongodb-community  # macOS\n     sudo systemctl start mongod           # Linux\n     ```\n3. **Verify installation** by running `mongosh` (MongoDB Shell) and typing `show dbs`.\n\n### **Option 2: Cloud MongoDB (Recommended for Production)**\nFor production apps, use **MongoDB Atlas** (free tier available):\n1. Sign up at [https://www.mongodb.com/atlas/database](https://www.mongodb.com/atlas/database).\n2. Create a **cluster**, then **create a database user** and **whitelist your IP**.\n3. Grab the **connection string** (e.g., `mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/?retryWrites=true&w=majority`).\n\n---\n\n## **2. Connecting Express to MongoDB with Mongoose**\n\nMongoose simplifies interactions with MongoDB by providing a schema-based structure. Let’s install and configure it.\n\n### **Install Mongoose**\n```bash\nnpm install mongoose\n```\n\n### **Configure the Database Connection**\nCreate a new file `config/db.js` in your project:\n\n```javascript\nconst mongoose = require('mongoose');\n\nconst connectDB = async () => {\n  try {\n    const conn = await mongoose.connect(process.env.MONGO_URI);\n    console.log(`MongoDB Connected: ${conn.connection.host}`);\n  } catch (error) {\n    console.error(`Error: ${error.message}`);\n    process.exit(1); // Exit process with failure\n  }\n};\n\nmodule.exports = connectDB;\n```\n\n### **Add MongoDB URI to `.env`**\nCreate a `.env` file in your project root:\n```env\nMONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/apiDB?retryWrites=true&w=majority\n```\n> **Never commit `.env` to Git!** Add it to `.gitignore`.\n\n### **Connect to MongoDB in `app.js`**\nModify your main `app.js` (or `server.js`) to load the database connection:\n\n```javascript\nconst express = require('express');\nconst connectDB = require('./config/db');\nconst app = express();\n\n// Connect to MongoDB\nconnectDB();\n\napp.get('/', (req, res) => {\n  res.send('API is running and connected to MongoDB!');\n});\n\nconst PORT = process.env.PORT || 5000;\napp.listen(PORT, () => console.log(`Server running on port ${PORT}`));\n```\n\nRun your app (`node app.js`). If successful, you’ll see:\n```\nMongoDB Connected: cluster0.example.mongodb.net\nServer running on port 5000\n```\n\n---\n\n## **3. Defining Mongoose Models**\n\nMongoose models define the structure of your documents in MongoDB. Let’s create a **Task** model as an example.\n\n### **Example: Task Model**\nCreate a file `models/Task.js`:\n\n```javascript\nconst mongoose = require('mongoose');\n\nconst TaskSchema = new mongoose.Schema({\n  title: {\n    type: String,\n    required: [true, 'Please add a title'],\n    trim: true,\n    maxlength: [50, 'Title cannot be more than 50 characters']\n  },\n  description: {\n    type: String,\n    required: false,\n    maxlength: [500, 'Description cannot exceed 500 characters']\n  },\n  completed: {\n    type: Boolean,\n    default: false\n  },\n  createdAt: {\n    type: Date,\n    default: Date.now\n  }\n});\n\nmodule.exports = mongoose.model('Task', TaskSchema);\n```\n\n### **Key Schema Options**\n- `required`: Ensures a field must be provided.\n- `trim`: Removes whitespace from strings.\n- `maxlength`/`minlength`: Validates string length.\n- `default`: Sets a default value if none is provided.\n\n---\n\n## **4. Performing CRUD Operations with Mongoose**\n\nNow that we have a model, let’s perform CRUD operations in a controller.\n\n### **Example: Task Controller (`controllers/taskController.js`)**\n```javascript\nconst Task = require('../models/Task');\n\n// @desc    Get all tasks\n// @route   GET /api/tasks\nconst getTasks = async (req, res) => {\n  try {\n    const tasks = await Task.find();\n    res.json(tasks);\n  } catch (error) {\n    res.status(500).json({ message: error.message });\n  }\n};\n\n// @desc    Create a new task\n// @route   POST /api/tasks\nconst createTask = async (req, res) => {\n  const { title, description } = req.body;\n\n  try {\n    const task = new Task({ title, description });\n    await task.save();\n    res.status(201).json(task);\n  } catch (error) {\n    res.status(400).json({ message: error.message });\n  }\n};\n\n// @desc    Update a task\n// @route   PUT /api/tasks/:id\nconst updateTask = async (req, res) => {\n  try {\n    const task = await Task.findByIdAndUpdate(\n      req.params.id,\n      req.body,\n      { new: true, runValidators: true }\n    );\n    if (!task) return res.status(404).json({ message: 'Task not found' });\n    res.json(task);\n  } catch (error) {\n    res.status(400).json({ message: error.message });\n  }\n};\n\n// @desc    Delete a task\n// @route   DELETE /api/tasks/:id\nconst deleteTask = async (req, res) => {\n  try {\n    const task = await Task.findByIdAndDelete(req.params.id);\n    if (!task) return res.status(404).json({ message: 'Task not found' });\n    res.json({ message: 'Task deleted successfully' });\n  } catch (error) {\n    res.status(500).json({ message: error.message });\n  }\n};\n\nmodule.exports = { getTasks, createTask, updateTask, deleteTask };\n```\n\n### **Add Routes (`routes/taskRoutes.js`)**\n```javascript\nconst express = require('express');\nconst router = express.Router();\nconst {\n  getTasks,\n  createTask,\n  updateTask,\n  deleteTask\n} = require('../controllers/taskController');\n\nrouter.route('/')\n  .get(getTasks)\n  .post(createTask);\n\nrouter.route('/:id')\n  .put(updateTask)\n  .delete(deleteTask);\n\nmodule.exports = router;\n```\n\n### **Mount the Routes in `app.js`**\n```javascript\nconst taskRoutes = require('./routes/taskRoutes');\napp.use('/api/tasks', taskRoutes);\n```\n\n---\n\n## **5. Testing the API with Postman**\n\nLet’s test our endpoints with **Postman**:\n\n| **Method** | **Endpoint**       | **Request Body (JSON)**                     | **Expected Response**                     |\n|------------|--------------------|---------------------------------------------|-------------------------------------------|\n| `GET`      | `/api/tasks`       | -                                           | `[{ \"title\": \"...\", \"completed\": false }]`|\n| `POST`     | `/api/tasks`       | `{ \"title\": \"Learn Mongoose\", \"description\": \"Study Mongoose ODM\" }` | `201 Created` + new task |\n| `PUT`      | `/api/tasks/:id`   | `{ \"completed\": true }`                     | Updated task                              |\n| `DELETE`   | `/api/tasks/:id`   | -                                           | `200 OK` + success message                |\n\n> **Pro Tip:** Use **MongoDB Compass** (GUI) to visually inspect your database:\n> ![MongoDB Compass UI](https://www.mongodb.com/docs/compass/current/_images/tutorials/connect.png)\n\n---\n\n## **Key Takeaways**\n- **MongoDB** is a flexible NoSQL database that stores data as JSON-like documents.\n- **Mongoose** provides an ODM layer, making it easier to interact with MongoDB using schemas and models.\n- **CRUD operations** (Create, Read, Update, Delete) are performed using Mongoose methods like `find()`, `save()`, `findByIdAndUpdate()`, and `findByIdAndDelete()`.",
      "wordCount": 955
    },
    {
      "title": "Module 6: Building a RESTful API",
      "content": "## Chapter 6: Building a RESTful API\n\nIn this chapter, we’ll design and implement a complete RESTful API using Express and MongoDB. You’ll learn how to structure your API endpoints according to REST principles, handle CRUD operations, and return proper HTTP status codes and response formats. By the end, you’ll have a functional API for a simple blog application.\n\n---\n\n### Understanding REST Principles\n\nREST (Representational State Transfer) is an architectural style for designing networked applications. A RESTful API adheres to these principles:\n\n1. **Statelessness**: Each request contains all the information needed to process it. The server doesn’t store client state between requests.\n2. **Resource Identification**: Use URLs to identify resources (e.g., `/posts`, `/users`).\n3. **HTTP Methods**: Use standard HTTP methods:\n   - `GET` to retrieve resources.\n   - `POST` to create resources.\n   - `PUT` or `PATCH` to update resources.\n   - `DELETE` to remove resources.\n4. **Representation**: Resources can have multiple representations (e.g., JSON, XML). We’ll use JSON.\n5. **Uniform Interface**: Consistency in how resources are accessed and manipulated.\n\n---\n\n### API Design Best Practices\n\nFollow these best practices when designing your API:\n\n1. **Use Nouns for Resources**: Avoid verbs in URLs. For example, use `/posts` instead of `/getPosts`.\n2. **Version Your API**: Start your URLs with `/api/v1/` to allow future changes without breaking clients.\n3. **Consistent Naming**: Use plural nouns for resources (e.g., `/posts`).\n4. **Filtering, Sorting, and Pagination**: Support query parameters like `?page=2&limit=10` for large datasets.\n5. **Authentication and Authorization**: Use middleware to protect routes (we’ll cover this in the next chapter).\n6. **Error Handling**: Return consistent error responses with appropriate HTTP status codes.\n\n---\n\n### Setting Up the Project\n\nLet’s create a simple blog API with two resources: `posts` and `comments`. We’ll use Express for the server and Mongoose to interact with MongoDB.\n\n1. **Initialize the Project**:\n   ```bash\n   mkdir blog-api\n   cd blog-api\n   npm init -y\n   npm install express mongoose dotenv\n   ```\n\n2. **Create `.env` File**:\n   ```env\n   PORT=3000\n   MONGODB_URI=mongodb://localhost:27017/blog-api\n   ```\n\n3. **Project Structure**:\n   ```\n   blog-api/\n   ├── .env\n   ├── app.js\n   ├── models/\n   │   ├── Post.js\n   │   └── Comment.js\n   ├── routes/\n   │   ├── posts.js\n   │   └── comments.js\n   └── controllers/\n       ├── postController.js\n       └── commentController.js\n   ```\n\n---\n\n### Defining the Schema\n\n#### Post Schema (`models/Post.js`)\n```javascript\nconst mongoose = require('mongoose');\n\nconst postSchema = new mongoose.Schema({\n  title: {\n    type: String,\n    required: true,\n    trim: true,\n  },\n  content: {\n    type: String,\n    required: true,\n  },\n  author: {\n    type: String,\n    required: true,\n  },\n  createdAt: {\n    type: Date,\n    default: Date.now,\n  },\n});\n\nmodule.exports = mongoose.model('Post', postSchema);\n```\n\n#### Comment Schema (`models/Comment.js`)\n```javascript\nconst mongoose = require('mongoose');\n\nconst commentSchema = new mongoose.Schema({\n  postId: {\n    type: mongoose.Schema.Types.ObjectId,\n    ref: 'Post',\n    required: true,\n  },\n  content: {\n    type: String,\n    required: true,\n  },\n  author: {\n    type: String,\n    required: true,\n  },\n  createdAt: {\n    type: Date,\n    default: Date.now,\n  },\n});\n\nmodule.exports = mongoose.model('Comment', commentSchema);\n```\n\n---\n\n### Implementing CRUD Operations\n\nWe’ll create controllers for each resource to handle the business logic and separate it from the routes.\n\n#### Post Controller (`controllers/postController.js`)\n```javascript\nconst Post = require('../models/Post');\n\n// Create a new post\nexports.createPost = async (req, res) => {\n  try {\n    const { title, content, author } = req.body;\n    const post = new Post({ title, content, author });\n    await post.save();\n    res.status(201).json(post);\n  } catch (error) {\n    res.status(400).json({ error: error.message });\n  }\n};\n\n// Get all posts\nexports.getAllPosts = async (req, res) => {\n  try {\n    const posts = await Post.find().sort({ createdAt: -1 });\n    res.json(posts);\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n};\n\n// Get a single post\nexports.getPostById = async (req, res) => {\n  try {\n    const post = await Post.findById(req.params.id);\n    if (!post) {\n      return res.status(404).json({ error: 'Post not found' });\n    }\n    res.json(post);\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n};\n\n// Update a post\nexports.updatePost = async (req, res) => {\n  try {\n    const post = await Post.findByIdAndUpdate(req.params.id, req.body, {\n      new: true,\n      runValidators: true,\n    });\n    if (!post) {\n      return res.status(404).json({ error: 'Post not found' });\n    }\n    res.json(post);\n  } catch (error) {\n    res.status(400).json({ error: error.message });\n  }\n};\n\n// Delete a post\nexports.deletePost = async (req, res) => {\n  try {\n    const post = await Post.findByIdAndDelete(req.params.id);\n    if (!post) {\n      return res.status(404).json({ error: 'Post not found' });\n    }\n    res.json({ message: 'Post deleted successfully' });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n};\n```\n\n#### Comment Controller (`controllers/commentController.js`)\n```javascript\nconst Comment = require('../models/Comment');\n\n// Create a comment for a post\nexports.createComment = async (req, res) => {\n  try {\n    const { postId, content, author } = req.body;\n    const comment = new Comment({ postId, content, author });\n    await comment.save();\n    res.status(201).json(comment);\n  } catch (error) {\n    res.status(400).json({ error: error.message });\n  }\n};\n\n// Get all comments for a post\nexports.getCommentsByPostId = async (req, res) => {\n  try {\n    const comments = await Comment.find({ postId: req.params.postId }).sort({\n      createdAt: -1,\n    });\n    res.json(comments);\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n};\n```\n\n---\n\n### Creating Routes\n\n#### Post Routes (`routes/posts.js`)\n```javascript\nconst express = require('express');\nconst router = express.Router();\nconst postController = require('../controllers/postController');\n\n// Create a post\nrouter.post('/', postController.createPost);\n\n// Get all posts\nrouter.get('/', postController.getAllPosts);\n\n// Get a single post\nrouter.get('/:id', postController.getPostById);\n\n// Update a post\nrouter.put('/:id', postController.updatePost);\n\n// Delete a post\nrouter.delete('/:id', postController.deletePost);\n\nmodule.exports = router;\n```\n\n#### Comment Routes (`routes/comments.js`)\n```javascript\nconst express = require('express');\nconst router = express.Router();\nconst commentController = require('../controllers/commentController');\n\n// Create a comment for a post\nrouter.post('/', commentController.createComment);\n\n// Get all comments for a post\nrouter.get('/:postId', commentController.getCommentsByPostId);\n\nmodule.exports = router;\n```\n\n---\n\n### Integrating Routes into the App\n\nIn your `app.js` file, integrate the routes and connect to MongoDB:\n\n```javascript\nconst express = require('express');\nconst mongoose = require('mongoose');\nconst dotenv = require('dotenv');\nconst postRoutes = require('./routes/posts');\nconst commentRoutes = require('./routes/comments');\n\ndotenv.config();\n\nconst app = express();\nconst PORT = process.env.PORT || 3000;\n\n// Middleware\napp.use(express.json());\n\n// Connect to MongoDB\nmongoose\n  .connect(process.env.MONGODB_URI)\n  .then(() => console.log('Connected to MongoDB'))\n  .catch((err) => console.error('MongoDB connection error:', err));\n\n// Routes\napp.use('/api/v1/posts', postRoutes);\napp.use('/api/v1/comments', commentRoutes);\n\n// Error handling middleware\napp.use((err, req, res, next) => {\n  console.error(err.stack);\n  res.status(500).json({ error: 'Something went wrong!' });\n});\n\napp.listen(PORT, () => {\n  console.log(`Server running on http://localhost:${PORT}`);\n});\n```\n\n---\n\n### Real-World Example: Creating a Blog Post\n\nLet’s create a new post using the API.\n\n1. **Request**:\n   ```bash\n   curl -X POST http://localhost:3000/api/v1/posts \\\n   -H \"Content-Type: application/json\" \\\n   -d '{\n     \"",
      "wordCount": 1022
    },
    {
      "title": "Module 7: Authentication and Authorization",
      "content": "## Module 7: Authentication and Authorization\n\nIn this module, you’ll learn how to secure your REST API by implementing authentication and authorization using JSON Web Tokens (JWT). We’ll cover user registration and login, token generation and validation, route protection, and role-based access control. By the end, your API will ensure that only authenticated users can access protected resources, and permissions will be enforced based on user roles.\n\n---\n\n## Introduction to Authentication and Authorization\n\n**Authentication** is the process of verifying a user’s identity, while **authorization** determines what authenticated users are allowed to do. In REST APIs, these concepts are crucial for protecting sensitive data and controlling access.\n\nFor this module, we’ll use:\n- **JWT (JSON Web Token)**: A compact, secure way to transmit information between parties as a JSON object.\n- **Express middleware**: To intercept requests and enforce authentication and authorization rules.\n- **Role-based access control (RBAC)**: To manage permissions based on user roles (e.g., admin, editor, user).\n\n---\n\n## Setting Up JWT Authentication\n\n### Install Required Dependencies\n\nFirst, install the necessary packages:\n```bash\nnpm install jsonwebtoken bcryptjs dotenv\n```\n\n- **jsonwebtoken**: For generating and verifying JWTs.\n- **bcryptjs**: For securely hashing passwords.\n- **dotenv**: For managing environment variables.\n\n### Create a User Model\n\nDefine a `User` model in `models/User.js`:\n```javascript\nconst mongoose = require('mongoose');\nconst bcrypt = require('bcryptjs');\n\nconst userSchema = new mongoose.Schema({\n  username: { type: String, required: true, unique: true },\n  email: { type: String, required: true, unique: true },\n  password: { type: String, required: true },\n  role: { type: String, enum: ['user', 'editor', 'admin'], default: 'user' }\n});\n\n// Hash password before saving\nuserSchema.pre('save', async function(next) {\n  if (!this.isModified('password')) return next();\n  this.password = await bcrypt.hash(this.password, 10);\n  next();\n});\n\n// Method to compare passwords\nuserSchema.methods.comparePassword = async function(candidatePassword) {\n  return await bcrypt.compare(candidatePassword, this.password);\n};\n\nmodule.exports = mongoose.model('User', userSchema);\n```\n\n### Create Auth Routes\n\nIn `routes/auth.js`, define routes for registration and login:\n```javascript\nconst express = require('express');\nconst jwt = require('jsonwebtoken');\nconst User = require('../models/User');\nconst router = express.Router();\nrequire('dotenv').config();\n\n// Register a new user\nrouter.post('/register', async (req, res) => {\n  try {\n    const { username, email, password } = req.body;\n    const user = new User({ username, email, password });\n    await user.save();\n    res.status(201).json({ message: 'User registered successfully' });\n  } catch (error) {\n    res.status(400).json({ error: error.message });\n  }\n});\n\n// Login and generate JWT\nrouter.post('/login', async (req, res) => {\n  try {\n    const { email, password } = req.body;\n    const user = await User.findOne({ email });\n    if (!user || !(await user.comparePassword(password))) {\n      return res.status(401).json({ error: 'Invalid credentials' });\n    }\n\n    // Generate JWT\n    const token = jwt.sign(\n      { id: user._id, role: user.role },\n      process.env.JWT_SECRET,\n      { expiresIn: '1h' }\n    );\n\n    res.json({ token });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\nmodule.exports = router;\n```\n\n### Protect Environment Variables\n\nCreate a `.env` file:\n```env\nJWT_SECRET=your_secure_jwt_secret_key_here\n```\n\nAdd `.env` to your `.gitignore` to keep secrets safe.\n\n---\n\n## Protecting Routes with Middleware\n\n### Create Authentication Middleware\n\nIn `middleware/auth.js`, define middleware to verify JWT:\n```javascript\nconst jwt = require('jsonwebtoken');\nrequire('dotenv').config();\n\nconst auth = (req, res, next) => {\n  const token = req.header('Authorization')?.replace('Bearer ', '');\n  if (!token) {\n    return res.status(401).json({ error: 'Access denied. No token provided.' });\n  }\n\n  try {\n    const decoded = jwt.verify(token, process.env.JWT_SECRET);\n    req.user = decoded; // Attach user data to request\n    next();\n  } catch (error) {\n    res.status(400).json({ error: 'Invalid token.' });\n  }\n};\n\nmodule.exports = auth;\n```\n\n### Create Authorization Middleware for Roles\n\nAdd role-based authorization middleware in `middleware/roles.js`:\n```javascript\nconst checkRole = (roles) => (req, res, next) => {\n  if (!req.user) {\n    return res.status(401).json({ error: 'Unauthorized' });\n  }\n\n  if (!roles.includes(req.user.role)) {\n    return res.status(403).json({ error: 'Forbidden. Insufficient permissions.' });\n  }\n\n  next();\n};\n\nmodule.exports = checkRole;\n```\n\n---\n\n## Example: Securing API Endpoints\n\n### Example 1: Protected User Profile Route\n\nIn `routes/profile.js`:\n```javascript\nconst express = require('express');\nconst auth = require('../middleware/auth');\nconst checkRole = require('../middleware/roles');\nconst router = express.Router();\n\n// Protected route - only authenticated users\nrouter.get('/me', auth, async (req, res) => {\n  try {\n    const user = await User.findById(req.user.id).select('-password');\n    res.json(user);\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\n// Admin-only route\nrouter.get('/admin-only', auth, checkRole(['admin']), (req, res) => {\n  res.json({ message: 'Welcome, admin!' });\n});\n\nmodule.exports = router;\n```\n\n### Example 2: Role-Based Access in a Blog API\n\nIn a blog API, you might have:\n```javascript\n// Create a post (only for editors and admins)\nrouter.post('/posts', auth, checkRole(['editor', 'admin']), async (req, res) => {\n  try {\n    const post = new Post({ ...req.body, author: req.user.id });\n    await post.save();\n    res.status(201).json(post);\n  } catch (error) {\n    res.status(400).json({ error: error.message });\n  }\n});\n\n// Delete a post (only for admins)\nrouter.delete('/posts/:id', auth, checkRole(['admin']), async (req, res) => {\n  try {\n    await Post.findByIdAndDelete(req.params.id);\n    res.json({ message: 'Post deleted' });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n```\n\n---\n\n## Best Practices for JWT Authentication\n\n1. **Use HTTPS**: Always serve your API over HTTPS to prevent token interception.\n2. **Short Expiry Times**: Set short expiration times for tokens and use refresh tokens for long-lived sessions.\n3. **Store Tokens Securely**: On the client, store JWTs in HTTP-only cookies or secure storage (not localStorage).\n4. **Validate Inputs**: Sanitize user inputs to prevent injection attacks.\n5. **Rate Limiting**: Implement rate limiting on auth endpoints to prevent brute-force attacks.\n\n---\n\n## Testing Your Authentication Flow\n\nYou can test your endpoints using **Postman** or **curl**:\n\n### Register a User\n```bash\ncurl -X POST http://localhost:3000/api/auth/register \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"username\": \"john_doe\", \"email\": \"john@example.com\", \"password\": \"password123\"}'\n```\n\n### Login and Get Token\n```bash\ncurl -X POST http://localhost:3000/api/auth/login \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"email\": \"john@example.com\", \"password\": \"password123\"}'\n```\n\n### Access Protected Route\n```bash\ncurl -X GET http://localhost:3000/api/profile/me \\\n  -H \"Authorization: Bearer YOUR_JWT_TOKEN\"\n```\n\n---\n\n## Key Takeaways\n\n- **JWT** is a secure and stateless way to handle authentication in REST APIs.\n- **Middleware** can be used to protect routes and enforce authorization rules.\n- **Role-based access control (RBAC)** allows you to manage permissions efficiently and granularly.\n\nBy implementing JWT authentication and role-based authorization, you’ve added a critical layer of security to your API, ensuring that only the right users can access the right resources.",
      "wordCount": 1005
    },
    {
      "title": "Module 8: Testing and Deployment",
      "content": "## **Chapter 8: Testing and Deployment – Ensuring Reliability and Scaling Your API**\n\nBuilding a REST API is only the first step—ensuring it works reliably and can scale under real-world conditions is equally critical. In this chapter, we’ll focus on **testing** your API thoroughly and **deploying** it to a cloud platform like **Render** or **Railway**, so you can confidently release your application to users.\n\nBy the end of this chapter, you’ll be able to:\n- Write unit and integration tests using **Jest** and **Postman**\n- Prepare your API for production\n- Deploy your API to a cloud service\n- Monitor its performance in the wild\n\nLet’s get started.\n\n---\n\n## **1. Why Testing Matters: Building Confidence in Your API**\n\nTesting is not optional—it’s a safety net that catches bugs before they reach production. A well-tested API:\n- Ensures correctness and reliability\n- Improves maintainability\n- Reduces the risk of breaking changes\n- Builds user trust\n\nWe’ll cover two main types of testing:\n- **Unit Tests**: Test individual functions or components in isolation\n- **Integration Tests**: Verify that different parts of your API work together correctly\n\n---\n\n## **2. Writing Unit Tests with Jest**\n\n[Jest](https://jestjs.io/) is a popular JavaScript testing framework that’s fast, easy to set up, and works great with Express APIs. Let’s walk through setting it up and writing tests.\n\n### **Step 1: Install Jest and Related Dependencies**\n\n```bash\nnpm install --save-dev jest supertest\n```\n\n- `jest`: The testing framework\n- `supertest`: Lets you test HTTP servers by making requests\n\n### **Step 2: Configure Jest**\n\nAdd a `jest.config.js` file in your project root:\n\n```js\nmodule.exports = {\n  testEnvironment: 'node',\n  clearMocks: true,\n};\n```\n\n### **Step 3: Write Your First Test**\n\nLet’s test a simple route handler from a previous chapter:\n\n```js\n// app.js\nconst express = require('express');\nconst app = express();\n\napp.get('/api/books', (req, res) => {\n  res.json([{ id: 1, title: 'The Great Gatsby' }, { id: 2, title: '1984' }]);\n});\n```\n\nNow, create a test file: `books.test.js`\n\n```js\nconst app = require('./app');\nconst request = require('supertest');\n\ndescribe('GET /api/books', () => {\n  it('should return an array of books', async () => {\n    const res = await request(app).get('/api/books');\n    expect(res.statusCode).toEqual(200);\n    expect(res.body).toBeInstanceOf(Array);\n    expect(res.body.length).toBeGreaterThan(0);\n  });\n});\n```\n\n### **Step 4: Run the Test**\n\nAdd a script to `package.json`:\n\n```json\n\"scripts\": {\n  \"test\": \"jest\"\n}\n```\n\nThen run:\n\n```bash\nnpm test\n```\n\n✅ You should see a passing test!\n\n---\n\n## **3. Writing Integration Tests with Postman**\n\nWhile Jest is great for backend testing, **Postman** is excellent for **API contract testing** and **integration testing** from the client’s perspective.\n\n### **Step 1: Set Up Postman**\n\n1. Download [Postman](https://www.postman.com/downloads/)\n2. Create a new collection for your API\n3. Add individual requests (GET, POST, etc.) with example payloads\n\n### **Step 2: Test a POST Endpoint**\n\nLet’s test a `/api/books` POST endpoint that creates a new book.\n\n**Request:**\n- Method: `POST`\n- URL: `http://localhost:3000/api/books`\n- Headers: `Content-Type: application/json`\n- Body (raw JSON):\n\n```json\n{\n  \"title\": \"The Hobbit\",\n  \"author\": \"J.R.R. Tolkien\"\n}\n```\n\n**Tests (in Postman):**\nAdd this script in the \"Tests\" tab:\n\n```js\npm.test(\"Status code is 201\", function() {\n  pm.response.to.have.status(201);\n});\n\npm.test(\"Response has an ID\", function() {\n  const jsonData = pm.response.json();\n  pm.expect(jsonData).to.have.property('id');\n});\n\npm.test(\"Title matches\", function() {\n  const jsonData = pm.response.json();\n  pm.expect(jsonData.title).to.eql(\"The Hobbit\");\n});\n```\n\n💡 **Pro Tip:** Use **Postman New Monitor** to run automated tests in the cloud on a schedule.\n\n---\n\n## **4. Preparing Your API for Production**\n\nBefore deploying, ensure your API is production-ready:\n\n### ✅ **Environment Variables**\nUse `dotenv` to manage environment-specific settings:\n\n```bash\nnpm install dotenv\n```\n\nCreate `.env`:\n```env\nPORT=3000\nMONGODB_URI=mongodb://localhost:27017/myapp\nJWT_SECRET=your-secret-key\n```\n\nUpdate `app.js`:\n```js\nrequire('dotenv').config();\nconst port = process.env.PORT || 3000;\n```\n\n### ✅ **Error Handling**\nAdd a global error handler:\n\n```js\napp.use((err, req, res, next) => {\n  console.error(err.stack);\n  res.status(500).json({ error: 'Something went wrong!' });\n});\n```\n\n### ✅ **Logging**\nUse `morgan` for HTTP request logging:\n\n```bash\nnpm install morgan\n```\n\n```js\nconst morgan = require('morgan');\napp.use(morgan('dev'));\n```\n\n### ✅ **Security**\n- Use `helmet` to set secure HTTP headers\n- Use `express-rate-limit` to prevent abuse\n- Sanitize user input (e.g., with `express-validator`)\n\n```bash\nnpm install helmet express-rate-limit express-validator\n```\n\n```js\nconst helmet = require('helmet');\nconst rateLimit = require('express-rate-limit');\n\napp.use(helmet());\nconst limiter = rateLimit({ windowMs: 15 * 60 * 1000, max: 100 });\napp.use(limiter);\n```\n\n### ✅ **Database Connection**\nEnsure your database connection is robust:\n\n```js\nconst mongoose = require('mongoose');\n\nmongoose.connect(process.env.MONGODB_URI)\n  .then(() => console.log('Connected to MongoDB'))\n  .catch(err => console.error('MongoDB connection error:', err));\n```\n\n---\n\n## **5. Deploying Your API to Render or Railway**\n\nNow, it’s time to go live! We’ll deploy to **Render** and **Railway**, two of the easiest platforms for Node.js apps.\n\n---\n\n### 🔹 **Deploying to Render**\n\n**Render** offers free tier hosting with automatic HTTPS and easy environment setup.\n\n#### **Step 1: Push Code to GitHub**\nMake sure your project is in a GitHub repository.\n\n#### **Step 2: Create a `render.yaml` (Optional)**\nRender can auto-detect a Node.js app, but you can create a config file:\n\n```yaml\nservices:\n  - type: web\n    name: node-api\n    env: node\n    buildCommand: npm install\n    startCommand: node app.js\n    envVars:\n      - key: MONGODB_URI\n        value: your_mongodb_uri\n      - key: JWT_SECRET\n        value: your_secret\n```\n\n#### **Step 3: Sign Up and Create a New Web Service**\n\n1. Go to [Render](https://render.com)\n2. Click “New” > “Web Service”\n3. Connect your GitHub repo\n4. Set the branch (usually `main`)\n5. Configure:\n   - **Name**: `node-api`\n   - **Region**: Choose closest to your users\n   - **Start Command**: `node app.js`\n   - **Environment**: Node\n6. Add environment variables from `.env` (keep secrets **hidden**—use Render’s secrets manager)\n\n#### **Step 4: Deploy!**\n\nRender will automatically build and deploy. You’ll get a live URL like:\n\n```\nhttps://node-api.onrender.com\n```\n\n---\n\n### 🔹 **Deploying to Railway**\n\n**Railway** is even simpler and great for beginners.\n\n#### **Step 1: Sign Up**\nGo to [Railway](https://railway.app) and sign up with GitHub.\n\n#### **Step 2: Create a New Project**\n1. Click “New Project” > “Deploy from GitHub repo”\n2. Select your repo\n3. Click “Deploy”\n\n#### **Step 3: Set Environment Variables**\nGo to **Variables** in the Railway dashboard and add:\n\n```\nPORT=3000\nMONGODB_URI=your_mongodb_uri\nJWT_SECRET=your_secret\n```\n\n#### **Step 4: Monitor and Scale**\nRailway automatically provides:\n- Logs\n- Metrics\n- HTTP requests\n- Auto-scaling\n\nYou’ll get a URL like:\n\n```\nhttps://node-api.up.railway.app\n```\n\n💡 **Note:** Both platforms offer free tiers with limitations. For production, consider upgrading.\n\n---\n\n## **6. Monitoring and Performance**\n\nOnce deployed, don’t forget to monitor your API.\n\n### ✅ **Logging**\nUse services like **Logflare**, **Papertrail**, or **Render’s built-in logs**.\n\n### ✅ **Uptime Monitoring**\nUse **UptimeRobot** or **Pingdom** to check if your",
      "wordCount": 1073
    }
  ],
  "finalBook": "# From Zero to Hero: Building REST APIs with Node.js and Express\n\n## Contents\n\n1. Module 1: Introduction to Node.js and NPM\n2. Module 2: Getting Started with Express.js\n3. Module 3: Working with Middleware\n4. Module 4: Handling Requests and Responses\n5. Module 5: Connecting to a Database\n6. Module 6: Building a RESTful API\n7. Module 7: Authentication and Authorization\n8. Module 8: Testing and Deployment\n\n\n---\n\n\n# Chapter 1: Module 1: Introduction to Node.js and NPM\n\n## **Chapter 1: Introduction to Node.js and NPM**\n\nWelcome to the first module of *Build REST APIs with Node.js and Express*! In this chapter, we’ll lay the foundation by understanding Node.js, its runtime environment, and how to set up your development environment using Node Package Manager (NPM). By the end, you’ll be ready to create your first Node.js project and manage dependencies efficiently.\n\n---\n\n## **What is Node.js?**\n\nNode.js is an open-source, cross-platform **JavaScript runtime environment** that allows you to execute JavaScript code outside a web browser. Unlike traditional JavaScript, which runs in the browser, Node.js runs on the server, enabling **server-side scripting** and the development of scalable network applications.\n\n### **Key Features of Node.js**\n1. **Event-Driven & Non-Blocking I/O**: Node.js uses an event loop to handle asynchronous operations efficiently, making it ideal for I/O-heavy applications (e.g., APIs, real-time chat apps).\n2. **Single-Threaded with Event Loop**: Despite being single-threaded, Node.js leverages an event-driven architecture to handle multiple requests concurrently.\n3. **JavaScript Everywhere**: With Node.js, you can use JavaScript for both **frontend** and **backend** development, reducing context-switching.\n4. **NPM (Node Package Manager)**: A vast ecosystem of reusable code packages (libraries/modules) that accelerates development.\n\n### **Use Cases for Node.js**\n- **RESTful APIs** (like the ones we’ll build in this course)\n- **Real-time applications** (e.g., chat apps, gaming servers)\n- **Microservices architecture**\n- **Serverless functions** (AWS Lambda, Firebase Functions)\n- **Command-line tools** (e.g., Babel, Webpack)\n\n**Example 1: Simple HTTP Server in Node.js**\nLet’s create a basic HTTP server to understand how Node.js handles requests:\n\n```javascript\n// server.js\nconst http = require('http');\n\nconst server = http.createServer((req, res) => {\n  res.writeHead(200, { 'Content-Type': 'text/plain' });\n  res.end('Hello from Node.js!');\n});\n\nserver.listen(3000, () => {\n  console.log('Server running on http://localhost:3000');\n});\n```\n**How it works:**\n1. We import Node’s built-in `http` module.\n2. We create a server that listens on port `3000`.\n3. When a request is made, the server responds with `\"Hello from Node.js!\"`.\n\nRun this code using:\n```bash\nnode server.js\n```\nVisit `http://localhost:3000` in your browser to see the response.\n\n---\n\n## **Installing Node.js and NPM**\n\n### **Step 1: Downloading Node.js**\nNode.js includes **NPM** (Node Package Manager) by default, so installing Node.js automatically installs NPM.\n\n1. **Download Node.js** from the [official website](https://nodejs.org/).\n   - Choose the **LTS (Long-Term Support)** version for stability.\n   - The installer includes NPM.\n\n2. **Run the installer** and follow the steps (default settings are fine).\n\n3. **Verify Installation**\n   Open a terminal (Command Prompt, PowerShell, or Terminal) and run:\n   ```bash\n   node -v\n   npm -v\n   ```\n   You should see the installed versions, e.g.:\n   ```\n   v20.12.2  # Node.js version\n   10.5.0    # NPM version\n   ```\n\n### **Step 2: Updating Node.js and NPM**\nTo update Node.js and NPM later, you can:\n- **Reinstall Node.js** from the website.\n- Use **NPM** to update itself:\n  ```bash\n  npm install -g npm@latest\n  ```\n- Use a version manager like **nvm** (Node Version Manager) for more control (recommended for advanced users).\n\n**Example 2: Using NPM to Install a Package**\nLet’s install `express`, the web framework we’ll use later:\n```bash\nnpm install express\n```\nThis downloads `express` and its dependencies into a `node_modules` folder.\n\n---\n\n## **Creating and Managing a Node.js Project with NPM**\n\nEvery Node.js project should have a `package.json` file, which acts as a **manifest** for your project. It stores metadata (name, version, dependencies) and scripts.\n\n### **Step 1: Initialize a Node.js Project**\n1. Create a new folder for your project:\n   ```bash\n   mkdir my-first-node-app\n   cd my-first-node-app\n   ```\n2. Initialize the project:\n   ```bash\n   npm init -y\n   ```\n   The `-y` flag skips the interactive questionnaire (default values are used).\n\n   This creates a `package.json` file:\n   ```json\n   {\n     \"name\": \"my-first-node-app\",\n     \"version\": \"1.0.0\",\n     \"description\": \"\",\n     \"main\": \"index.js\",\n     \"scripts\": {\n       \"test\": \"echo \\\"Error: no test specified\\\" && exit 1\"\n     },\n     \"keywords\": [],\n     \"author\": \"\",\n     \"license\": \"ISC\"\n   }\n   ```\n\n### **Step 2: Adding Dependencies**\nTo add a package (e.g., `express`):\n```bash\nnpm install express\n```\nThis:\n1. Downloads `express` into `node_modules`.\n2. Adds `express` to `dependencies` in `package.json`:\n   ```json\n   \"dependencies\": {\n     \"express\": \"^4.18.2\"\n   }\n   ```\n\n### **Step 3: Running Scripts**\nYou can define custom scripts in `package.json` under `\"scripts\"`. For example:\n```json\n\"scripts\": {\n  \"start\": \"node server.js\",\n  \"dev\": \"nodemon server.js\"\n}\n```\n- Run `npm start` to start your app.\n- Run `npm run dev` if you have `nodemon` installed (a tool that auto-restarts the server on file changes).\n\n### **Step 4: Managing `node_modules`**\n- **Install all dependencies** listed in `package.json`:\n  ```bash\n  npm install\n  ```\n- **Remove a package**:\n  ```bash\n  npm uninstall express\n  ```\n- **Save a package as a `devDependency`** (used only in development):\n  ```bash\n  npm install nodemon --save-dev\n  ```\n\n---\n\n## **Understanding the Node.js Runtime Environment**\n\nNode.js runs on the **V8 JavaScript engine** (the same engine used in Chrome), which compiles JavaScript to native machine code for fast execution. Key components of Node.js include:\n\n1. **Event Loop**: Handles asynchronous operations (e.g., file I/O, network requests) without blocking the main thread.\n2. **Modules**: Node.js uses the `require()` function to import built-in modules (e.g., `http`, `fs`) or third-party packages.\n3. **Process Object**: Provides information about the running Node.js process (e.g., `process.env` for environment variables).\n4. **Buffer Class**: Handles binary data (important for file operations and network streams).\n\n---\n\n## **Summary**\nIn this chapter, we:\n1. Learned what Node.js is and why it’s powerful.\n2. Installed Node.js and verified the installation.\n3. Created a Node.js project and managed dependencies with NPM.\n\nYou now have a working Node.js environment ready for building APIs!\n\n---\n\n## **Key Takeaways**\n- **Node.js** is a JavaScript runtime for server-side development, enabling scalable, event-driven applications.\n- **NPM** is the default package manager for Node.js, allowing you to install, update, and manage libraries (packages).\n- A **Node.js project** is defined by its `package.json` file, which tracks dependencies and scripts.\n\n---\n\n# Chapter 2: Module 2: Getting Started with Express.js\n\n## Module 2: Getting Started with Express.js\n\nExpress.js is a minimal and flexible web application framework for Node.js that provides a robust set of features to build web and mobile applications. In this module, you'll learn how to set up a basic Express server, understand routing fundamentals, and handle HTTP methods effectively. By the end, you'll have a solid foundation to start building RESTful APIs with Express.\n\n---\n\n### **Setting Up Express.js in a Node.js Project**\n\nBefore diving into Express, ensure you have Node.js installed on your system. You can verify this by running:\n\n```bash\nnode -v\nnpm -v\n```\n\nIf Node.js is not installed, download it from [nodejs.org](https://nodejs.org/).\n\n#### **Step 1: Initialize a Node.js Project**\nCreate a new directory for your project and initialize it with npm:\n\n```bash\nmkdir express-api\ncd express-api\nnpm init -y\n```\n\nThis creates a `package.json` file with default settings.\n\n#### **Step 2: Install Express.js**\nInstall Express as a dependency in your project:\n\n```bash\nnpm install express\n```\n\nThis adds Express to your `node_modules` and updates the `package.json` file.\n\n#### **Step 3: Create a Basic Server**\nCreate a file named `app.js` (or `index.js`) in your project root. This will be the entry point of your application. Add the following code to set up a basic Express server:\n\n```javascript\n// Import the express module\nconst express = require('express');\n\n// Create an instance of an Express application\nconst app = express();\n\n// Define the port number\nconst PORT = 3000;\n\n// Define a route for the root path ('/')\napp.get('/', (req, res) => {\n    res.send('Welcome to the Home Page!');\n});\n\n// Start the server\napp.listen(PORT, () => {\n    console.log(`Server is running on http://localhost:${PORT}`);\n});\n```\n\n#### **Step 4: Run the Server**\nExecute your server using Node.js:\n\n```bash\nnode app.js\n```\n\nOpen your web browser and navigate to `http://localhost:3000`. You should see the message \"Welcome to the Home Page!\" displayed.\n\n#### **Step 5: Enhance the Server with Middleware**\nExpress.js middleware functions have access to the request (`req`), response (`res`), and the next middleware function in the cycle. Middleware can modify the request and response objects, end the request-response cycle, or call the next middleware function.\n\nA common middleware is `express.json()`, which parses incoming JSON requests:\n\n```javascript\n// Add JSON parsing middleware\napp.use(express.json());\n\n// Example route that accepts JSON data\napp.post('/api/data', (req, res) => {\n    const receivedData = req.body;\n    res.json({\n        message: 'Data received successfully!',\n        data: receivedData\n    });\n});\n```\n\nNow, if you send a POST request to `http://localhost:3000/api/data` with a JSON payload, the server will respond with the received data.\n\n---\n\n### **Understanding Routing Fundamentals**\n\nRouting refers to how an application’s endpoints (URIs) respond to client requests. In Express, routes are defined using HTTP methods (e.g., `GET`, `POST`, `PUT`, `DELETE`) and path patterns.\n\n#### **Basic Route Structure**\nA basic route is defined as:\n\n```javascript\napp.METHOD(PATH, HANDLER);\n```\n\n- `METHOD`: The HTTP method (e.g., `get`, `post`, `put`, `delete`).\n- `PATH`: The route path (e.g., `'/'`, `/users`, `/products/:id`).\n- `HANDLER`: The function executed when the route is matched.\n\n#### **Route Parameters**\nRoute parameters are named URL segments used to capture values from the URL. For example, if you have a route like `/users/:userId`, `:userId` is a route parameter that can be accessed via `req.params`.\n\n```javascript\n// Example route with a parameter\napp.get('/users/:userId', (req, res) => {\n    const userId = req.params.userId;\n    res.send(`User ID: ${userId}`);\n});\n```\n\nIf you visit `http://localhost:3000/users/123`, the server will respond with \"User ID: 123\".\n\n#### **Handling Different HTTP Methods**\nExpress supports all HTTP methods. Here are examples for common methods:\n\n##### **GET Request**\nRetrieve data from the server.\n\n```javascript\n// Example: Fetch a list of products\napp.get('/products', (req, res) => {\n    const products = [\n        { id: 1, name: 'Laptop' },\n        { id: 2, name: 'Phone' },\n        { id: 3, name: 'Tablet' }\n    ];\n    res.json(products);\n});\n```\n\n##### **POST Request**\nSubmit data to the server to create a new resource.\n\n```javascript\n// Example: Create a new user\napp.post('/users', (req, res) => {\n    const newUser = {\n        id: 4,\n        name: req.body.name,\n        email: req.body.email\n    };\n    // In a real app, you'd save this to a database\n    res.status(201).json(newUser);\n});\n```\n\n##### **PUT/PATCH Request**\nUpdate an existing resource.\n\n```javascript\n// Example: Update a user's email\napp.put('/users/:userId', (req, res) => {\n    const userId = req.params.userId;\n    const updatedUser = {\n        id: userId,\n        name: req.body.name,\n        email: req.body.email\n    };\n    res.json(updatedUser);\n});\n```\n\n##### **DELETE Request**\nRemove a resource from the server.\n\n```javascript\n// Example: Delete a user\napp.delete('/users/:userId', (req, res) => {\n    const userId = req.params.userId;\n    res.status(204).send(); // No content, successful deletion\n});\n```\n\n---\n\n### **Practical Example: Building a Simple REST API**\n\nLet's put everything together by creating a simple REST API for managing a list of books.\n\n#### **Step 1: Define the Book Data**\nCreate an array to store book data temporarily.\n\n```javascript\nlet books = [\n    { id: 1, title: 'The Great Gatsby', author: 'F. Scott Fitzgerald' },\n    { id: 2, title: '1984', author: 'George Orwell' },\n    { id: 3, title: 'To Kill a Mockingbird', author: 'Harper Lee' }\n];\n```\n\n#### **Step 2: Define Routes**\nAdd the following routes to your `app.js`:\n\n```javascript\n// Middleware to parse JSON bodies\napp.use(express.json());\n\n// GET all books\napp.get('/books', (req, res) => {\n    res.json(books);\n});\n\n// GET a single book by ID\napp.get('/books/:id', (req, res) => {\n    const bookId = parseInt(req.params.id);\n    const book = books.find(b => b.id === bookId);\n\n    if (book) {\n        res.json(book);\n    } else {\n        res.status(404).json({ message: 'Book not found' });\n    }\n});\n\n// POST a new book\napp.post('/books', (req, res) => {\n    const newBook = {\n        id: books.length + 1,\n        title: req.body.title,\n        author: req.body.author\n    };\n    books.push(newBook);\n    res.status(201).json(newBook);\n});\n\n// PUT (update) a book\napp.put('/books/:id', (req, res) => {\n    const bookId = parseInt(req.params.id);\n    const bookIndex = books.findIndex(b => b.id === bookId);\n\n    if (bookIndex !== -1) {\n        const updatedBook = {\n            id: bookId,\n            title: req.body.title,\n            author: req.body.author\n        };\n        books[bookIndex] = updatedBook;\n        res.json(updatedBook);\n    } else {\n        res.status(404).json({ message: 'Book not found' });\n    }\n});\n\n// DELETE a book\napp.delete('/books/:id', (req, res) => {\n    const bookId = parseInt(req.params.id);\n    const bookIndex = books.findIndex(b => b.id === bookId);\n\n    if (bookIndex !== -1) {\n        books.splice(bookIndex, 1);\n        res.status(204).send();\n    } else {\n        res.status(404).json({ message: 'Book not found' });\n    }\n});\n```\n\n#### **Step 3: Test the API**\nYou can test the API using tools like [Postman](https://www.postman.com/), [Insomnia](https://insomnia.rest/), or even the command line with `curl`.\n\nFor example, to get all books using `curl`:\n\n```bash\ncurl http://localhost:3000/books\n```\n\nTo create a new book:\n\n```bash\ncurl -X POST http://localhost:3000/books \\\n-H \"Content-Type: application/json\" \\\n-d '{\"title\": \"The Hobbit\", \"author\": \"J.R.R. Tolkien\"}'\n```\n\n---\n\n## Key Takeaways\n\n- **Express.js Simplifies Server Creation**: With Express, you can set up a web server in just a few lines of code using methods like `app.get()`, `app.post()`, etc.\n- **Routing is Central to Express**: Routes in Express are defined using HTTP methods and path patterns, making it easy to handle different\n\n---\n\n# Chapter 3: Module 3: Working with Middleware\n\n## Module 3: Working with Middleware\n\nExpress.js is a minimal and flexible Node.js web application framework that provides a robust set of features for web and mobile applications. One of the core concepts that make Express powerful is **middleware**. Middleware functions are functions that have access to the request object (`req`), the response object (`res`), and the next middleware function in the application’s request-response cycle. In this chapter, we’ll explore what middleware is, how it works in Express, and how to use both built-in and custom middleware effectively.\n\n---\n\n## Understanding Middleware in Express\n\n### What Is Middleware?\n\nMiddleware is software that sits between an operating system and the applications running on it, acting as a translator or filter. In the context of Express, middleware functions are software that process incoming requests before they reach the final route handler. They can:\n\n- Execute any code.\n- Make changes to the request and response objects.\n- End the request-response cycle (e.g., by sending a response).\n- Call the next middleware function in the stack.\n\nThink of middleware as a series of checkpoints in a pipeline. Each middleware function has the chance to inspect, modify, or reject requests before they reach the final destination.\n\n### The Request-Response Cycle with Middleware\n\nWhen a client sends a request to your Express app:\n\n1. The request enters the middleware pipeline.\n2. Each middleware function can process the request or pass it to the next function.\n3. If a middleware ends the response (e.g., by sending a response), the cycle stops.\n4. If all middleware passes the request, it reaches the final route handler.\n\nHere’s a simple example:\n\n```javascript\nconst express = require('express');\nconst app = express();\n\n// Middleware 1: Logs the time of the request\napp.use((req, res, next) => {\n  console.log(`Time: ${Date.now()}`);\n  next(); // Pass control to the next middleware\n});\n\n// Middleware 2: Logs the HTTP method and URL\napp.use((req, res, next) => {\n  console.log(`${req.method} request for ${req.url}`);\n  next();\n});\n\n// Route handler\napp.get('/', (req, res) => {\n  res.send('Hello World!');\n});\n\napp.listen(3000, () => {\n  console.log('Server running on port 3000');\n});\n```\n\nWhen you run this app and visit `http://localhost:3000/`, you’ll see two log lines in your console:\n```\nTime: 1712345678901\nGET request for /\nHello World!\n```\n\nThis shows how two middleware functions processed the request before it reached the final route handler.\n\n---\n\n## Using Built-in Middleware\n\nExpress comes with several built-in middleware functions that handle common tasks like parsing request bodies, serving static files, and handling CORS. Let's look at the most commonly used ones.\n\n---\n\n### 1. `express.json()` – Parsing JSON Requests\n\nWhen a client sends a JSON payload in the body of a POST or PUT request, you need to parse it into a JavaScript object before accessing its data. The `express.json()` middleware does exactly that.\n\n**How to Use:**\n\n```javascript\nconst express = require('express');\nconst app = express();\n\n// Use the json middleware to parse JSON request bodies\napp.use(express.json());\n\napp.post('/users', (req, res) => {\n  const user = req.body; // req.body is now a parsed JavaScript object\n  console.log('New user:', user);\n  res.status(201).json({ message: 'User created', user });\n});\n\napp.listen(3000, () => {\n  console.log('Server running on port 3000');\n});\n```\n\n**Example Request:**\n\n```bash\ncurl -X POST http://localhost:3000/users \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"name\": \"Alice\", \"email\": \"alice@example.com\"}'\n```\n\n**Output:**\n```\nNew user: { name: 'Alice', email: 'alice@example.com' }\n```\n\nThis middleware is essential for REST APIs that accept JSON input.\n\n---\n\n### 2. `express.urlencoded()` – Parsing URL-encoded Data\n\nThis middleware parses incoming requests with URL-encoded payloads (e.g., from HTML forms). It converts the data into a JavaScript object and attaches it to `req.body`.\n\n```javascript\napp.use(express.urlencoded({ extended: true }));\n\napp.post('/login', (req, res) => {\n  const { username, password } = req.body;\n  console.log(`Login attempt: ${username}`);\n  res.send(`Welcome, ${username}!`);\n});\n```\n\n> 🔍 **Note:** The `{ extended: true }` option allows parsing of rich objects and arrays (using the `qs` library), while `{ extended: false }` uses the simpler `querystring` library.\n\n---\n\n## Creating Custom Middleware Functions\n\nWhile built-in middleware covers common use cases, real-world applications often require custom logic. You can create your own middleware functions that perform specific tasks.\n\n### Structure of a Custom Middleware\n\nA custom middleware function takes three parameters:\n\n```javascript\n(req, res, next) => { ... }\n```\n\n- `req`: The request object.\n- `res`: The response object.\n- `next`: A function to pass control to the next middleware.\n\nIf you don’t call `next()`, the request will hang unless you send a response.\n\n---\n\n### Example 1: Authentication Middleware\n\nLet’s create a simple authentication middleware that checks for a valid API key.\n\n```javascript\n// Middleware to check API key\nconst authenticate = (req, res, next) => {\n  const apiKey = req.headers['x-api-key'];\n\n  if (apiKey && apiKey === 'SECRET123') {\n    // User is authenticated\n    req.user = { id: 1, name: 'Admin' };\n    next(); // Proceed to the next middleware\n  } else {\n    // Not authorized\n    res.status(401).json({ error: 'Unauthorized: Invalid API key' });\n  }\n};\n\n// Use the middleware on protected routes\napp.get('/dashboard', authenticate, (req, res) => {\n  res.json({ message: `Welcome to your dashboard, ${req.user.name}!` });\n});\n```\n\n**Test with:**\n\n```bash\ncurl http://localhost:3000/dashboard\n# Response: 401 Unauthorized\n\ncurl -H \"x-api-key: SECRET123\" http://localhost:3000/dashboard\n# Response: { \"message\": \"Welcome to your dashboard, Admin!\" }\n```\n\nThis is a simple example, but it demonstrates how middleware can enforce security rules.\n\n---\n\n### Example 2: Request Logging Middleware\n\nAnother common use case is logging requests for debugging or monitoring.\n\n```javascript\nconst requestLogger = (req, res, next) => {\n  const timestamp = new Date().toISOString();\n  const method = req.method;\n  const url = req.url;\n\n  console.log(`[${timestamp}] ${method} ${url}`);\n\n  // Optionally log IP and user agent\n  console.log(`IP: ${req.ip}, User-Agent: ${req.get('User-Agent')}`);\n\n  next(); // Always call next() unless you want to block the request\n};\n\napp.use(requestLogger); // Apply to all routes\n```\n\nThis middleware logs every incoming request with a timestamp, method, URL, IP, and user agent, which is helpful for debugging and analytics.\n\n---\n\n## Advanced Middleware Concepts\n\n### Order Matters\n\nMiddleware functions are executed in the order they are added. This is crucial—if you place authentication middleware after a route, it won’t protect that route.\n\n```javascript\napp.use(express.json()); // Parse JSON first\n\n// Authentication middleware\napp.use(authenticate); // Then check auth\n\n// Then define routes\napp.get('/profile', (req, res) => {\n  res.json({ user: req.user });\n});\n```\n\nIf you reverse the order, users won’t be authenticated when they reach `/profile`.\n\n---\n\n### Using Middleware on Specific Routes\n\nYou can apply middleware to specific routes using `.use()` or directly in route handlers.\n\n```javascript\n// Middleware only for /admin routes\napp.use('/admin', adminAuthMiddleware);\n\n// Or in a route\napp.get('/account', authMiddleware, (req, res) => {\n  // Only authenticated users reach here\n});\n```\n\n---\n\n### Error-handling Middleware\n\nExpress supports special middleware functions with **four parameters** to handle errors:\n\n```javascript\n(app, err, req, res, next) => { ... }\n```\n\n```javascript\n// Error-handling middleware\napp.use((err, req, res, next) => {\n  console.error(err.stack);\n  res.status(500).json({ error: 'Something went wrong!' });\n});\n```\n\n> ⚠️ Error middleware must be defined **after all other middleware and routes**.\n\n---\n\n## Best Practices\n\n1. **Keep Middleware Small and Focused**: Each middleware should do one thing well.\n2. **Use Middleware for Cross-cutting Concerns**: Logging, authentication, CORS, etc.\n3. **Order Matters**: Place middleware in the correct sequence.\n4. **Avoid Blocking the Event Loop**: Don’t perform CPU-intensive tasks in middleware.\n5. **Test Middleware Independently**: Write unit tests for custom middleware.\n\n---\n\n## Real-World Example: Building a RESTful API with Middleware\n\nLet’s build a small REST API for a blog with the following features:\n\n- Parse JSON and URL-encoded data.\n- Log all requests.\n- Validate API keys.\n- Handle errors globally.\n\n```javascript\nconst express = require('express');\nconst app =\n\n---\n\n# Chapter 4: Module 4: Handling Requests and Responses\n\n## Handling Requests and Responses in Express\n\nIn the previous chapters, we set up a basic Express server and learned how to handle routes. Now, we'll dive deeper into processing incoming requests and sending appropriate responses. This is a core skill for building REST APIs, as it determines how your application interacts with clients.\n\n### Understanding the Request Object\n\nEvery time a client makes a request to your server, Express creates a `Request` object containing all the information sent by the client. This object includes:\n\n- Route parameters (e.g., `/users/:id`)\n- Query strings (e.g., `?name=John&age=30`)\n- Request body (e.g., JSON data from POST requests)\n- Headers (e.g., `Content-Type`, `Authorization`)\n- HTTP method (GET, POST, PUT, DELETE, etc.)\n\nLet's see how to extract and use each of these components.\n\n---\n\n### Extracting Route Parameters\n\nRoute parameters are dynamic segments in your URL that capture values from the request path. For example, in the route `/products/:id`, `:id` is a parameter that matches whatever comes after `/products/` (e.g., `/products/123`).\n\n```javascript\nconst express = require('express');\nconst app = express();\n\n// Define a route with a parameter\napp.get('/products/:id', (req, res) => {\n  const productId = req.params.id; // Extract the 'id' parameter\n  res.send(`You requested product with ID: ${productId}`);\n});\n\napp.listen(3000, () => {\n  console.log('Server running on http://localhost:3000');\n});\n```\n\nIn this example:\n- A GET request to `/products/42` will respond with `\"You requested product with ID: 42\"`.\n- The value `42` is extracted from the URL and stored in `req.params.id`.\n\nRoute parameters are especially useful for retrieving specific resources, like fetching a user by their ID.\n\n---\n\n### Working with Query Strings\n\nQuery strings are key-value pairs appended to the URL after a `?` (e.g., `/search?q=express&sort=desc`). They are commonly used for filtering, sorting, or pagination in APIs.\n\n```javascript\napp.get('/search', (req, res) => {\n  const query = req.query; // Extract the query object\n  res.json({\n    query: query.q,\n    sort: query.sort,\n    message: `Searching for \"${query.q}\" with sort order \"${query.sort}\"`\n  });\n});\n```\n\nA request to `/search?q=rest&sort=asc` would respond with:\n\n```json\n{\n  \"query\": \"rest\",\n  \"sort\": \"asc\",\n  \"message\": \"Searching for \\\"rest\\\" with sort order \\\"asc\\\"\"\n}\n```\n\nTo handle optional query parameters, always check if they exist before using them:\n\n```javascript\napp.get('/users', (req, res) => {\n  const limit = req.query.limit || 10; // Default to 10 if not provided\n  res.send(`Returning up to ${limit} users`);\n});\n```\n\n---\n\n### Accessing the Request Body\n\nTo handle data sent in the request body (e.g., from POST or PUT requests), you need to use middleware that parses the body. Express has built-in middleware for JSON and URL-encoded data.\n\n#### Parsing JSON Data\n\nFor APIs that send JSON data (common in REST APIs), use `express.json()`:\n\n```javascript\napp.use(express.json()); // Middleware to parse JSON bodies\n\napp.post('/users', (req, res) => {\n  const user = req.body; // Access the parsed JSON data\n  console.log('New user:', user);\n  res.status(201).send(`User ${user.name} created!`);\n});\n```\n\nA POST request with the body:\n\n```json\n{\n  \"name\": \"Alice\",\n  \"email\": \"alice@example.com\"\n}\n```\n\nWould respond with:\n\n```\nUser Alice created!\n```\n\n> ⚠️ **Important**: Always use `express.json()` (or equivalent middleware) before defining routes that expect a body, or the body will be `undefined`.\n\n#### Parsing URL-Encoded Data\n\nFor form submissions (e.g., `application/x-www-form-urlencoded`), use `express.urlencoded()`:\n\n```javascript\napp.use(express.urlencoded({ extended: true }));\n\napp.post('/login', (req, res) => {\n  const { username, password } = req.body;\n  // Validate credentials...\n  res.send(`Welcome, ${username}!`);\n});\n```\n\n---\n\n### Sending Responses\n\nExpress provides several methods to send responses to the client. The most common are:\n\n- `res.send()` – Sends a response of various types (string, object, buffer)\n- `res.json()` – Sends a JSON response\n- `res.status(code).send()` – Sets HTTP status code and sends response\n- `res.redirect()` – Redirects to another URL\n\n#### Example: Sending JSON Responses\n\n```javascript\napp.get('/api/user/1', (req, res) => {\n  const user = {\n    id: 1,\n    name: \"John Doe\",\n    email: \"john@example.com\"\n  };\n  res.json(user); // Automatically sets Content-Type: application/json\n});\n```\n\n#### Example: Using Status Codes\n\nAlways use appropriate HTTP status codes:\n\n```javascript\napp.post('/orders', (req, res) => {\n  const order = createOrder(req.body); // Assume this creates an order\n  res.status(201).json({\n    message: 'Order created',\n    orderId: order.id\n  });\n});\n```\n\nCommon status codes:\n- `200 OK` – Success\n- `201 Created` – Resource created\n- `400 Bad Request` – Invalid client input\n- `404 Not Found` – Resource doesn't exist\n- `500 Internal Server Error` – Server-side error\n\n---\n\n### Handling Errors and Sending Error Responses\n\nErrors are inevitable in any application. The key is to handle them gracefully and send meaningful feedback to the client.\n\n#### Basic Error Handling\n\n```javascript\napp.get('/divide', (req, res) => {\n  const a = parseInt(req.query.a);\n  const b = parseInt(req.query.b);\n\n  if (isNaN(a) || isNaN(b)) {\n    return res.status(400).json({\n      error: \"Invalid input: 'a' and 'b' must be numbers.\"\n    });\n  }\n\n  if (b === 0) {\n    return res.status(400).json({\n      error: \"Division by zero is not allowed.\"\n    });\n  }\n\n  const result = a / b;\n  res.json({ result });\n});\n```\n\nA request to `/divide?a=10&b=2` returns:\n```json\n{ \"result\": 5 }\n```\n\nA request to `/divide?a=10&b=0` returns:\n```json\n{ \"error\": \"Division by zero is not allowed.\" }\n```\n\n> ✅ **Tip**: Always `return` after sending a response in error handlers to stop further execution.\n\n---\n\n### Using Middleware for Consistent Error Handling\n\nFor a cleaner approach, define an error-handling middleware. Express recognizes error-handling middleware by its four parameters: `(err, req, res, next)`.\n\n```javascript\n// Error-handling middleware\napp.use((err, req, res, next) => {\n  console.error(err.stack);\n  res.status(500).json({\n    error: 'Something went wrong!',\n    message: process.env.NODE_ENV === 'development' ? err.message : undefined\n  });\n});\n\n// Example route that throws an error\napp.get('/risky', (req, res, next) => {\n  try {\n    throw new Error('Oops! Something failed.');\n  } catch (err) {\n    next(err); // Pass error to the error handler\n  }\n});\n```\n\nIn development, this might return:\n```json\n{\n  \"error\": \"Something went wrong!\",\n  \"message\": \"Oops! Something failed.\"\n}\n```\n\nIn production, sensitive details are hidden.\n\n---\n\n### Real-World Example: Building a Simple User API\n\nLet's put it all together in a small user management API.\n\n```javascript\nconst express = require('express');\nconst app = express();\n\n// Middleware\napp.use(express.json());\n\n// In-memory \"database\"\nlet users = [\n  { id: 1, name: 'Alice', email: 'alice@example.com' },\n  { id: 2, name: 'Bob', email: 'bob@example.com' }\n];\n\n// GET /users – List all users or filter by query\napp.get('/users', (req, res) => {\n  const name = req.query.name?.toLowerCase();\n  const filteredUsers = name\n    ? users.filter(u => u.name.toLowerCase().includes(name))\n    : users;\n\n  res.json(filteredUsers);\n});\n\n// GET /users/:id – Get a single user\napp.get('/users/:id', (req, res) => {\n  const userId = parseInt(req.params.id);\n  const user = users.find(u => u.id === userId);\n\n  if (!user) {\n    return res.status(404).json({ error: 'User not found' });\n  }\n\n  res.json(user);\n});\n\n// POST /users – Create a new user\napp.post('/users', (req, res) => {\n  const { name, email } = req.body;\n\n  if (!name || !email) {\n    return res.status(400).json({ error: 'Name and email are required' });\n  }\n\n  const newUser = {\n    id: users.length\n\n---\n\n# Chapter 5: Module 5: Connecting to a Database\n\n## **Chapter 5: Connecting to a Database – Integrating MongoDB with Mongoose**\n\nBuilding a REST API is only half the battle—what good is your API if it can’t store or retrieve data? In this chapter, we’ll connect your Express application to a **MongoDB** database using **Mongoose**, a popular Object Data Modeling (ODM) library for Node.js. By the end, you’ll know how to set up MongoDB, define data models, and perform CRUD operations with ease.\n\n---\n\n## **1. Setting Up MongoDB**\n\nMongoDB is a NoSQL database that stores data in flexible, JSON-like documents. Before we connect it to our API, we need to set it up.\n\n### **Option 1: Local MongoDB Installation**\n1. **Install MongoDB Community Server** from [https://www.mongodb.com/try/download/community](https://www.mongodb.com/try/download/community).\n2. **Start the MongoDB service**:\n   - On **Windows**, run `mongod` in the Command Prompt.\n   - On **macOS/Linux**, use:\n     ```bash\n     brew services start mongodb-community  # macOS\n     sudo systemctl start mongod           # Linux\n     ```\n3. **Verify installation** by running `mongosh` (MongoDB Shell) and typing `show dbs`.\n\n### **Option 2: Cloud MongoDB (Recommended for Production)**\nFor production apps, use **MongoDB Atlas** (free tier available):\n1. Sign up at [https://www.mongodb.com/atlas/database](https://www.mongodb.com/atlas/database).\n2. Create a **cluster**, then **create a database user** and **whitelist your IP**.\n3. Grab the **connection string** (e.g., `mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/?retryWrites=true&w=majority`).\n\n---\n\n## **2. Connecting Express to MongoDB with Mongoose**\n\nMongoose simplifies interactions with MongoDB by providing a schema-based structure. Let’s install and configure it.\n\n### **Install Mongoose**\n```bash\nnpm install mongoose\n```\n\n### **Configure the Database Connection**\nCreate a new file `config/db.js` in your project:\n\n```javascript\nconst mongoose = require('mongoose');\n\nconst connectDB = async () => {\n  try {\n    const conn = await mongoose.connect(process.env.MONGO_URI);\n    console.log(`MongoDB Connected: ${conn.connection.host}`);\n  } catch (error) {\n    console.error(`Error: ${error.message}`);\n    process.exit(1); // Exit process with failure\n  }\n};\n\nmodule.exports = connectDB;\n```\n\n### **Add MongoDB URI to `.env`**\nCreate a `.env` file in your project root:\n```env\nMONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/apiDB?retryWrites=true&w=majority\n```\n> **Never commit `.env` to Git!** Add it to `.gitignore`.\n\n### **Connect to MongoDB in `app.js`**\nModify your main `app.js` (or `server.js`) to load the database connection:\n\n```javascript\nconst express = require('express');\nconst connectDB = require('./config/db');\nconst app = express();\n\n// Connect to MongoDB\nconnectDB();\n\napp.get('/', (req, res) => {\n  res.send('API is running and connected to MongoDB!');\n});\n\nconst PORT = process.env.PORT || 5000;\napp.listen(PORT, () => console.log(`Server running on port ${PORT}`));\n```\n\nRun your app (`node app.js`). If successful, you’ll see:\n```\nMongoDB Connected: cluster0.example.mongodb.net\nServer running on port 5000\n```\n\n---\n\n## **3. Defining Mongoose Models**\n\nMongoose models define the structure of your documents in MongoDB. Let’s create a **Task** model as an example.\n\n### **Example: Task Model**\nCreate a file `models/Task.js`:\n\n```javascript\nconst mongoose = require('mongoose');\n\nconst TaskSchema = new mongoose.Schema({\n  title: {\n    type: String,\n    required: [true, 'Please add a title'],\n    trim: true,\n    maxlength: [50, 'Title cannot be more than 50 characters']\n  },\n  description: {\n    type: String,\n    required: false,\n    maxlength: [500, 'Description cannot exceed 500 characters']\n  },\n  completed: {\n    type: Boolean,\n    default: false\n  },\n  createdAt: {\n    type: Date,\n    default: Date.now\n  }\n});\n\nmodule.exports = mongoose.model('Task', TaskSchema);\n```\n\n### **Key Schema Options**\n- `required`: Ensures a field must be provided.\n- `trim`: Removes whitespace from strings.\n- `maxlength`/`minlength`: Validates string length.\n- `default`: Sets a default value if none is provided.\n\n---\n\n## **4. Performing CRUD Operations with Mongoose**\n\nNow that we have a model, let’s perform CRUD operations in a controller.\n\n### **Example: Task Controller (`controllers/taskController.js`)**\n```javascript\nconst Task = require('../models/Task');\n\n// @desc    Get all tasks\n// @route   GET /api/tasks\nconst getTasks = async (req, res) => {\n  try {\n    const tasks = await Task.find();\n    res.json(tasks);\n  } catch (error) {\n    res.status(500).json({ message: error.message });\n  }\n};\n\n// @desc    Create a new task\n// @route   POST /api/tasks\nconst createTask = async (req, res) => {\n  const { title, description } = req.body;\n\n  try {\n    const task = new Task({ title, description });\n    await task.save();\n    res.status(201).json(task);\n  } catch (error) {\n    res.status(400).json({ message: error.message });\n  }\n};\n\n// @desc    Update a task\n// @route   PUT /api/tasks/:id\nconst updateTask = async (req, res) => {\n  try {\n    const task = await Task.findByIdAndUpdate(\n      req.params.id,\n      req.body,\n      { new: true, runValidators: true }\n    );\n    if (!task) return res.status(404).json({ message: 'Task not found' });\n    res.json(task);\n  } catch (error) {\n    res.status(400).json({ message: error.message });\n  }\n};\n\n// @desc    Delete a task\n// @route   DELETE /api/tasks/:id\nconst deleteTask = async (req, res) => {\n  try {\n    const task = await Task.findByIdAndDelete(req.params.id);\n    if (!task) return res.status(404).json({ message: 'Task not found' });\n    res.json({ message: 'Task deleted successfully' });\n  } catch (error) {\n    res.status(500).json({ message: error.message });\n  }\n};\n\nmodule.exports = { getTasks, createTask, updateTask, deleteTask };\n```\n\n### **Add Routes (`routes/taskRoutes.js`)**\n```javascript\nconst express = require('express');\nconst router = express.Router();\nconst {\n  getTasks,\n  createTask,\n  updateTask,\n  deleteTask\n} = require('../controllers/taskController');\n\nrouter.route('/')\n  .get(getTasks)\n  .post(createTask);\n\nrouter.route('/:id')\n  .put(updateTask)\n  .delete(deleteTask);\n\nmodule.exports = router;\n```\n\n### **Mount the Routes in `app.js`**\n```javascript\nconst taskRoutes = require('./routes/taskRoutes');\napp.use('/api/tasks', taskRoutes);\n```\n\n---\n\n## **5. Testing the API with Postman**\n\nLet’s test our endpoints with **Postman**:\n\n| **Method** | **Endpoint**       | **Request Body (JSON)**                     | **Expected Response**                     |\n|------------|--------------------|---------------------------------------------|-------------------------------------------|\n| `GET`      | `/api/tasks`       | -                                           | `[{ \"title\": \"...\", \"completed\": false }]`|\n| `POST`     | `/api/tasks`       | `{ \"title\": \"Learn Mongoose\", \"description\": \"Study Mongoose ODM\" }` | `201 Created` + new task |\n| `PUT`      | `/api/tasks/:id`   | `{ \"completed\": true }`                     | Updated task                              |\n| `DELETE`   | `/api/tasks/:id`   | -                                           | `200 OK` + success message                |\n\n> **Pro Tip:** Use **MongoDB Compass** (GUI) to visually inspect your database:\n> ![MongoDB Compass UI](https://www.mongodb.com/docs/compass/current/_images/tutorials/connect.png)\n\n---\n\n## **Key Takeaways**\n- **MongoDB** is a flexible NoSQL database that stores data as JSON-like documents.\n- **Mongoose** provides an ODM layer, making it easier to interact with MongoDB using schemas and models.\n- **CRUD operations** (Create, Read, Update, Delete) are performed using Mongoose methods like `find()`, `save()`, `findByIdAndUpdate()`, and `findByIdAndDelete()`.\n\n---\n\n# Chapter 6: Module 6: Building a RESTful API\n\n## Chapter 6: Building a RESTful API\n\nIn this chapter, we’ll design and implement a complete RESTful API using Express and MongoDB. You’ll learn how to structure your API endpoints according to REST principles, handle CRUD operations, and return proper HTTP status codes and response formats. By the end, you’ll have a functional API for a simple blog application.\n\n---\n\n### Understanding REST Principles\n\nREST (Representational State Transfer) is an architectural style for designing networked applications. A RESTful API adheres to these principles:\n\n1. **Statelessness**: Each request contains all the information needed to process it. The server doesn’t store client state between requests.\n2. **Resource Identification**: Use URLs to identify resources (e.g., `/posts`, `/users`).\n3. **HTTP Methods**: Use standard HTTP methods:\n   - `GET` to retrieve resources.\n   - `POST` to create resources.\n   - `PUT` or `PATCH` to update resources.\n   - `DELETE` to remove resources.\n4. **Representation**: Resources can have multiple representations (e.g., JSON, XML). We’ll use JSON.\n5. **Uniform Interface**: Consistency in how resources are accessed and manipulated.\n\n---\n\n### API Design Best Practices\n\nFollow these best practices when designing your API:\n\n1. **Use Nouns for Resources**: Avoid verbs in URLs. For example, use `/posts` instead of `/getPosts`.\n2. **Version Your API**: Start your URLs with `/api/v1/` to allow future changes without breaking clients.\n3. **Consistent Naming**: Use plural nouns for resources (e.g., `/posts`).\n4. **Filtering, Sorting, and Pagination**: Support query parameters like `?page=2&limit=10` for large datasets.\n5. **Authentication and Authorization**: Use middleware to protect routes (we’ll cover this in the next chapter).\n6. **Error Handling**: Return consistent error responses with appropriate HTTP status codes.\n\n---\n\n### Setting Up the Project\n\nLet’s create a simple blog API with two resources: `posts` and `comments`. We’ll use Express for the server and Mongoose to interact with MongoDB.\n\n1. **Initialize the Project**:\n   ```bash\n   mkdir blog-api\n   cd blog-api\n   npm init -y\n   npm install express mongoose dotenv\n   ```\n\n2. **Create `.env` File**:\n   ```env\n   PORT=3000\n   MONGODB_URI=mongodb://localhost:27017/blog-api\n   ```\n\n3. **Project Structure**:\n   ```\n   blog-api/\n   ├── .env\n   ├── app.js\n   ├── models/\n   │   ├── Post.js\n   │   └── Comment.js\n   ├── routes/\n   │   ├── posts.js\n   │   └── comments.js\n   └── controllers/\n       ├── postController.js\n       └── commentController.js\n   ```\n\n---\n\n### Defining the Schema\n\n#### Post Schema (`models/Post.js`)\n```javascript\nconst mongoose = require('mongoose');\n\nconst postSchema = new mongoose.Schema({\n  title: {\n    type: String,\n    required: true,\n    trim: true,\n  },\n  content: {\n    type: String,\n    required: true,\n  },\n  author: {\n    type: String,\n    required: true,\n  },\n  createdAt: {\n    type: Date,\n    default: Date.now,\n  },\n});\n\nmodule.exports = mongoose.model('Post', postSchema);\n```\n\n#### Comment Schema (`models/Comment.js`)\n```javascript\nconst mongoose = require('mongoose');\n\nconst commentSchema = new mongoose.Schema({\n  postId: {\n    type: mongoose.Schema.Types.ObjectId,\n    ref: 'Post',\n    required: true,\n  },\n  content: {\n    type: String,\n    required: true,\n  },\n  author: {\n    type: String,\n    required: true,\n  },\n  createdAt: {\n    type: Date,\n    default: Date.now,\n  },\n});\n\nmodule.exports = mongoose.model('Comment', commentSchema);\n```\n\n---\n\n### Implementing CRUD Operations\n\nWe’ll create controllers for each resource to handle the business logic and separate it from the routes.\n\n#### Post Controller (`controllers/postController.js`)\n```javascript\nconst Post = require('../models/Post');\n\n// Create a new post\nexports.createPost = async (req, res) => {\n  try {\n    const { title, content, author } = req.body;\n    const post = new Post({ title, content, author });\n    await post.save();\n    res.status(201).json(post);\n  } catch (error) {\n    res.status(400).json({ error: error.message });\n  }\n};\n\n// Get all posts\nexports.getAllPosts = async (req, res) => {\n  try {\n    const posts = await Post.find().sort({ createdAt: -1 });\n    res.json(posts);\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n};\n\n// Get a single post\nexports.getPostById = async (req, res) => {\n  try {\n    const post = await Post.findById(req.params.id);\n    if (!post) {\n      return res.status(404).json({ error: 'Post not found' });\n    }\n    res.json(post);\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n};\n\n// Update a post\nexports.updatePost = async (req, res) => {\n  try {\n    const post = await Post.findByIdAndUpdate(req.params.id, req.body, {\n      new: true,\n      runValidators: true,\n    });\n    if (!post) {\n      return res.status(404).json({ error: 'Post not found' });\n    }\n    res.json(post);\n  } catch (error) {\n    res.status(400).json({ error: error.message });\n  }\n};\n\n// Delete a post\nexports.deletePost = async (req, res) => {\n  try {\n    const post = await Post.findByIdAndDelete(req.params.id);\n    if (!post) {\n      return res.status(404).json({ error: 'Post not found' });\n    }\n    res.json({ message: 'Post deleted successfully' });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n};\n```\n\n#### Comment Controller (`controllers/commentController.js`)\n```javascript\nconst Comment = require('../models/Comment');\n\n// Create a comment for a post\nexports.createComment = async (req, res) => {\n  try {\n    const { postId, content, author } = req.body;\n    const comment = new Comment({ postId, content, author });\n    await comment.save();\n    res.status(201).json(comment);\n  } catch (error) {\n    res.status(400).json({ error: error.message });\n  }\n};\n\n// Get all comments for a post\nexports.getCommentsByPostId = async (req, res) => {\n  try {\n    const comments = await Comment.find({ postId: req.params.postId }).sort({\n      createdAt: -1,\n    });\n    res.json(comments);\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n};\n```\n\n---\n\n### Creating Routes\n\n#### Post Routes (`routes/posts.js`)\n```javascript\nconst express = require('express');\nconst router = express.Router();\nconst postController = require('../controllers/postController');\n\n// Create a post\nrouter.post('/', postController.createPost);\n\n// Get all posts\nrouter.get('/', postController.getAllPosts);\n\n// Get a single post\nrouter.get('/:id', postController.getPostById);\n\n// Update a post\nrouter.put('/:id', postController.updatePost);\n\n// Delete a post\nrouter.delete('/:id', postController.deletePost);\n\nmodule.exports = router;\n```\n\n#### Comment Routes (`routes/comments.js`)\n```javascript\nconst express = require('express');\nconst router = express.Router();\nconst commentController = require('../controllers/commentController');\n\n// Create a comment for a post\nrouter.post('/', commentController.createComment);\n\n// Get all comments for a post\nrouter.get('/:postId', commentController.getCommentsByPostId);\n\nmodule.exports = router;\n```\n\n---\n\n### Integrating Routes into the App\n\nIn your `app.js` file, integrate the routes and connect to MongoDB:\n\n```javascript\nconst express = require('express');\nconst mongoose = require('mongoose');\nconst dotenv = require('dotenv');\nconst postRoutes = require('./routes/posts');\nconst commentRoutes = require('./routes/comments');\n\ndotenv.config();\n\nconst app = express();\nconst PORT = process.env.PORT || 3000;\n\n// Middleware\napp.use(express.json());\n\n// Connect to MongoDB\nmongoose\n  .connect(process.env.MONGODB_URI)\n  .then(() => console.log('Connected to MongoDB'))\n  .catch((err) => console.error('MongoDB connection error:', err));\n\n// Routes\napp.use('/api/v1/posts', postRoutes);\napp.use('/api/v1/comments', commentRoutes);\n\n// Error handling middleware\napp.use((err, req, res, next) => {\n  console.error(err.stack);\n  res.status(500).json({ error: 'Something went wrong!' });\n});\n\napp.listen(PORT, () => {\n  console.log(`Server running on http://localhost:${PORT}`);\n});\n```\n\n---\n\n### Real-World Example: Creating a Blog Post\n\nLet’s create a new post using the API.\n\n1. **Request**:\n   ```bash\n   curl -X POST http://localhost:3000/api/v1/posts \\\n   -H \"Content-Type: application/json\" \\\n   -d '{\n     \"\n\n---\n\n# Chapter 7: Module 7: Authentication and Authorization\n\n## Module 7: Authentication and Authorization\n\nIn this module, you’ll learn how to secure your REST API by implementing authentication and authorization using JSON Web Tokens (JWT). We’ll cover user registration and login, token generation and validation, route protection, and role-based access control. By the end, your API will ensure that only authenticated users can access protected resources, and permissions will be enforced based on user roles.\n\n---\n\n## Introduction to Authentication and Authorization\n\n**Authentication** is the process of verifying a user’s identity, while **authorization** determines what authenticated users are allowed to do. In REST APIs, these concepts are crucial for protecting sensitive data and controlling access.\n\nFor this module, we’ll use:\n- **JWT (JSON Web Token)**: A compact, secure way to transmit information between parties as a JSON object.\n- **Express middleware**: To intercept requests and enforce authentication and authorization rules.\n- **Role-based access control (RBAC)**: To manage permissions based on user roles (e.g., admin, editor, user).\n\n---\n\n## Setting Up JWT Authentication\n\n### Install Required Dependencies\n\nFirst, install the necessary packages:\n```bash\nnpm install jsonwebtoken bcryptjs dotenv\n```\n\n- **jsonwebtoken**: For generating and verifying JWTs.\n- **bcryptjs**: For securely hashing passwords.\n- **dotenv**: For managing environment variables.\n\n### Create a User Model\n\nDefine a `User` model in `models/User.js`:\n```javascript\nconst mongoose = require('mongoose');\nconst bcrypt = require('bcryptjs');\n\nconst userSchema = new mongoose.Schema({\n  username: { type: String, required: true, unique: true },\n  email: { type: String, required: true, unique: true },\n  password: { type: String, required: true },\n  role: { type: String, enum: ['user', 'editor', 'admin'], default: 'user' }\n});\n\n// Hash password before saving\nuserSchema.pre('save', async function(next) {\n  if (!this.isModified('password')) return next();\n  this.password = await bcrypt.hash(this.password, 10);\n  next();\n});\n\n// Method to compare passwords\nuserSchema.methods.comparePassword = async function(candidatePassword) {\n  return await bcrypt.compare(candidatePassword, this.password);\n};\n\nmodule.exports = mongoose.model('User', userSchema);\n```\n\n### Create Auth Routes\n\nIn `routes/auth.js`, define routes for registration and login:\n```javascript\nconst express = require('express');\nconst jwt = require('jsonwebtoken');\nconst User = require('../models/User');\nconst router = express.Router();\nrequire('dotenv').config();\n\n// Register a new user\nrouter.post('/register', async (req, res) => {\n  try {\n    const { username, email, password } = req.body;\n    const user = new User({ username, email, password });\n    await user.save();\n    res.status(201).json({ message: 'User registered successfully' });\n  } catch (error) {\n    res.status(400).json({ error: error.message });\n  }\n});\n\n// Login and generate JWT\nrouter.post('/login', async (req, res) => {\n  try {\n    const { email, password } = req.body;\n    const user = await User.findOne({ email });\n    if (!user || !(await user.comparePassword(password))) {\n      return res.status(401).json({ error: 'Invalid credentials' });\n    }\n\n    // Generate JWT\n    const token = jwt.sign(\n      { id: user._id, role: user.role },\n      process.env.JWT_SECRET,\n      { expiresIn: '1h' }\n    );\n\n    res.json({ token });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\nmodule.exports = router;\n```\n\n### Protect Environment Variables\n\nCreate a `.env` file:\n```env\nJWT_SECRET=your_secure_jwt_secret_key_here\n```\n\nAdd `.env` to your `.gitignore` to keep secrets safe.\n\n---\n\n## Protecting Routes with Middleware\n\n### Create Authentication Middleware\n\nIn `middleware/auth.js`, define middleware to verify JWT:\n```javascript\nconst jwt = require('jsonwebtoken');\nrequire('dotenv').config();\n\nconst auth = (req, res, next) => {\n  const token = req.header('Authorization')?.replace('Bearer ', '');\n  if (!token) {\n    return res.status(401).json({ error: 'Access denied. No token provided.' });\n  }\n\n  try {\n    const decoded = jwt.verify(token, process.env.JWT_SECRET);\n    req.user = decoded; // Attach user data to request\n    next();\n  } catch (error) {\n    res.status(400).json({ error: 'Invalid token.' });\n  }\n};\n\nmodule.exports = auth;\n```\n\n### Create Authorization Middleware for Roles\n\nAdd role-based authorization middleware in `middleware/roles.js`:\n```javascript\nconst checkRole = (roles) => (req, res, next) => {\n  if (!req.user) {\n    return res.status(401).json({ error: 'Unauthorized' });\n  }\n\n  if (!roles.includes(req.user.role)) {\n    return res.status(403).json({ error: 'Forbidden. Insufficient permissions.' });\n  }\n\n  next();\n};\n\nmodule.exports = checkRole;\n```\n\n---\n\n## Example: Securing API Endpoints\n\n### Example 1: Protected User Profile Route\n\nIn `routes/profile.js`:\n```javascript\nconst express = require('express');\nconst auth = require('../middleware/auth');\nconst checkRole = require('../middleware/roles');\nconst router = express.Router();\n\n// Protected route - only authenticated users\nrouter.get('/me', auth, async (req, res) => {\n  try {\n    const user = await User.findById(req.user.id).select('-password');\n    res.json(user);\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\n// Admin-only route\nrouter.get('/admin-only', auth, checkRole(['admin']), (req, res) => {\n  res.json({ message: 'Welcome, admin!' });\n});\n\nmodule.exports = router;\n```\n\n### Example 2: Role-Based Access in a Blog API\n\nIn a blog API, you might have:\n```javascript\n// Create a post (only for editors and admins)\nrouter.post('/posts', auth, checkRole(['editor', 'admin']), async (req, res) => {\n  try {\n    const post = new Post({ ...req.body, author: req.user.id });\n    await post.save();\n    res.status(201).json(post);\n  } catch (error) {\n    res.status(400).json({ error: error.message });\n  }\n});\n\n// Delete a post (only for admins)\nrouter.delete('/posts/:id', auth, checkRole(['admin']), async (req, res) => {\n  try {\n    await Post.findByIdAndDelete(req.params.id);\n    res.json({ message: 'Post deleted' });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n```\n\n---\n\n## Best Practices for JWT Authentication\n\n1. **Use HTTPS**: Always serve your API over HTTPS to prevent token interception.\n2. **Short Expiry Times**: Set short expiration times for tokens and use refresh tokens for long-lived sessions.\n3. **Store Tokens Securely**: On the client, store JWTs in HTTP-only cookies or secure storage (not localStorage).\n4. **Validate Inputs**: Sanitize user inputs to prevent injection attacks.\n5. **Rate Limiting**: Implement rate limiting on auth endpoints to prevent brute-force attacks.\n\n---\n\n## Testing Your Authentication Flow\n\nYou can test your endpoints using **Postman** or **curl**:\n\n### Register a User\n```bash\ncurl -X POST http://localhost:3000/api/auth/register \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"username\": \"john_doe\", \"email\": \"john@example.com\", \"password\": \"password123\"}'\n```\n\n### Login and Get Token\n```bash\ncurl -X POST http://localhost:3000/api/auth/login \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"email\": \"john@example.com\", \"password\": \"password123\"}'\n```\n\n### Access Protected Route\n```bash\ncurl -X GET http://localhost:3000/api/profile/me \\\n  -H \"Authorization: Bearer YOUR_JWT_TOKEN\"\n```\n\n---\n\n## Key Takeaways\n\n- **JWT** is a secure and stateless way to handle authentication in REST APIs.\n- **Middleware** can be used to protect routes and enforce authorization rules.\n- **Role-based access control (RBAC)** allows you to manage permissions efficiently and granularly.\n\nBy implementing JWT authentication and role-based authorization, you’ve added a critical layer of security to your API, ensuring that only the right users can access the right resources.\n\n---\n\n# Chapter 8: Module 8: Testing and Deployment\n\n## **Chapter 8: Testing and Deployment – Ensuring Reliability and Scaling Your API**\n\nBuilding a REST API is only the first step—ensuring it works reliably and can scale under real-world conditions is equally critical. In this chapter, we’ll focus on **testing** your API thoroughly and **deploying** it to a cloud platform like **Render** or **Railway**, so you can confidently release your application to users.\n\nBy the end of this chapter, you’ll be able to:\n- Write unit and integration tests using **Jest** and **Postman**\n- Prepare your API for production\n- Deploy your API to a cloud service\n- Monitor its performance in the wild\n\nLet’s get started.\n\n---\n\n## **1. Why Testing Matters: Building Confidence in Your API**\n\nTesting is not optional—it’s a safety net that catches bugs before they reach production. A well-tested API:\n- Ensures correctness and reliability\n- Improves maintainability\n- Reduces the risk of breaking changes\n- Builds user trust\n\nWe’ll cover two main types of testing:\n- **Unit Tests**: Test individual functions or components in isolation\n- **Integration Tests**: Verify that different parts of your API work together correctly\n\n---\n\n## **2. Writing Unit Tests with Jest**\n\n[Jest](https://jestjs.io/) is a popular JavaScript testing framework that’s fast, easy to set up, and works great with Express APIs. Let’s walk through setting it up and writing tests.\n\n### **Step 1: Install Jest and Related Dependencies**\n\n```bash\nnpm install --save-dev jest supertest\n```\n\n- `jest`: The testing framework\n- `supertest`: Lets you test HTTP servers by making requests\n\n### **Step 2: Configure Jest**\n\nAdd a `jest.config.js` file in your project root:\n\n```js\nmodule.exports = {\n  testEnvironment: 'node',\n  clearMocks: true,\n};\n```\n\n### **Step 3: Write Your First Test**\n\nLet’s test a simple route handler from a previous chapter:\n\n```js\n// app.js\nconst express = require('express');\nconst app = express();\n\napp.get('/api/books', (req, res) => {\n  res.json([{ id: 1, title: 'The Great Gatsby' }, { id: 2, title: '1984' }]);\n});\n```\n\nNow, create a test file: `books.test.js`\n\n```js\nconst app = require('./app');\nconst request = require('supertest');\n\ndescribe('GET /api/books', () => {\n  it('should return an array of books', async () => {\n    const res = await request(app).get('/api/books');\n    expect(res.statusCode).toEqual(200);\n    expect(res.body).toBeInstanceOf(Array);\n    expect(res.body.length).toBeGreaterThan(0);\n  });\n});\n```\n\n### **Step 4: Run the Test**\n\nAdd a script to `package.json`:\n\n```json\n\"scripts\": {\n  \"test\": \"jest\"\n}\n```\n\nThen run:\n\n```bash\nnpm test\n```\n\n✅ You should see a passing test!\n\n---\n\n## **3. Writing Integration Tests with Postman**\n\nWhile Jest is great for backend testing, **Postman** is excellent for **API contract testing** and **integration testing** from the client’s perspective.\n\n### **Step 1: Set Up Postman**\n\n1. Download [Postman](https://www.postman.com/downloads/)\n2. Create a new collection for your API\n3. Add individual requests (GET, POST, etc.) with example payloads\n\n### **Step 2: Test a POST Endpoint**\n\nLet’s test a `/api/books` POST endpoint that creates a new book.\n\n**Request:**\n- Method: `POST`\n- URL: `http://localhost:3000/api/books`\n- Headers: `Content-Type: application/json`\n- Body (raw JSON):\n\n```json\n{\n  \"title\": \"The Hobbit\",\n  \"author\": \"J.R.R. Tolkien\"\n}\n```\n\n**Tests (in Postman):**\nAdd this script in the \"Tests\" tab:\n\n```js\npm.test(\"Status code is 201\", function() {\n  pm.response.to.have.status(201);\n});\n\npm.test(\"Response has an ID\", function() {\n  const jsonData = pm.response.json();\n  pm.expect(jsonData).to.have.property('id');\n});\n\npm.test(\"Title matches\", function() {\n  const jsonData = pm.response.json();\n  pm.expect(jsonData.title).to.eql(\"The Hobbit\");\n});\n```\n\n💡 **Pro Tip:** Use **Postman New Monitor** to run automated tests in the cloud on a schedule.\n\n---\n\n## **4. Preparing Your API for Production**\n\nBefore deploying, ensure your API is production-ready:\n\n### ✅ **Environment Variables**\nUse `dotenv` to manage environment-specific settings:\n\n```bash\nnpm install dotenv\n```\n\nCreate `.env`:\n```env\nPORT=3000\nMONGODB_URI=mongodb://localhost:27017/myapp\nJWT_SECRET=your-secret-key\n```\n\nUpdate `app.js`:\n```js\nrequire('dotenv').config();\nconst port = process.env.PORT || 3000;\n```\n\n### ✅ **Error Handling**\nAdd a global error handler:\n\n```js\napp.use((err, req, res, next) => {\n  console.error(err.stack);\n  res.status(500).json({ error: 'Something went wrong!' });\n});\n```\n\n### ✅ **Logging**\nUse `morgan` for HTTP request logging:\n\n```bash\nnpm install morgan\n```\n\n```js\nconst morgan = require('morgan');\napp.use(morgan('dev'));\n```\n\n### ✅ **Security**\n- Use `helmet` to set secure HTTP headers\n- Use `express-rate-limit` to prevent abuse\n- Sanitize user input (e.g., with `express-validator`)\n\n```bash\nnpm install helmet express-rate-limit express-validator\n```\n\n```js\nconst helmet = require('helmet');\nconst rateLimit = require('express-rate-limit');\n\napp.use(helmet());\nconst limiter = rateLimit({ windowMs: 15 * 60 * 1000, max: 100 });\napp.use(limiter);\n```\n\n### ✅ **Database Connection**\nEnsure your database connection is robust:\n\n```js\nconst mongoose = require('mongoose');\n\nmongoose.connect(process.env.MONGODB_URI)\n  .then(() => console.log('Connected to MongoDB'))\n  .catch(err => console.error('MongoDB connection error:', err));\n```\n\n---\n\n## **5. Deploying Your API to Render or Railway**\n\nNow, it’s time to go live! We’ll deploy to **Render** and **Railway**, two of the easiest platforms for Node.js apps.\n\n---\n\n### 🔹 **Deploying to Render**\n\n**Render** offers free tier hosting with automatic HTTPS and easy environment setup.\n\n#### **Step 1: Push Code to GitHub**\nMake sure your project is in a GitHub repository.\n\n#### **Step 2: Create a `render.yaml` (Optional)**\nRender can auto-detect a Node.js app, but you can create a config file:\n\n```yaml\nservices:\n  - type: web\n    name: node-api\n    env: node\n    buildCommand: npm install\n    startCommand: node app.js\n    envVars:\n      - key: MONGODB_URI\n        value: your_mongodb_uri\n      - key: JWT_SECRET\n        value: your_secret\n```\n\n#### **Step 3: Sign Up and Create a New Web Service**\n\n1. Go to [Render](https://render.com)\n2. Click “New” > “Web Service”\n3. Connect your GitHub repo\n4. Set the branch (usually `main`)\n5. Configure:\n   - **Name**: `node-api`\n   - **Region**: Choose closest to your users\n   - **Start Command**: `node app.js`\n   - **Environment**: Node\n6. Add environment variables from `.env` (keep secrets **hidden**—use Render’s secrets manager)\n\n#### **Step 4: Deploy!**\n\nRender will automatically build and deploy. You’ll get a live URL like:\n\n```\nhttps://node-api.onrender.com\n```\n\n---\n\n### 🔹 **Deploying to Railway**\n\n**Railway** is even simpler and great for beginners.\n\n#### **Step 1: Sign Up**\nGo to [Railway](https://railway.app) and sign up with GitHub.\n\n#### **Step 2: Create a New Project**\n1. Click “New Project” > “Deploy from GitHub repo”\n2. Select your repo\n3. Click “Deploy”\n\n#### **Step 3: Set Environment Variables**\nGo to **Variables** in the Railway dashboard and add:\n\n```\nPORT=3000\nMONGODB_URI=your_mongodb_uri\nJWT_SECRET=your_secret\n```\n\n#### **Step 4: Monitor and Scale**\nRailway automatically provides:\n- Logs\n- Metrics\n- HTTP requests\n- Auto-scaling\n\nYou’ll get a URL like:\n\n```\nhttps://node-api.up.railway.app\n```\n\n💡 **Note:** Both platforms offer free tiers with limitations. For production, consider upgrading.\n\n---\n\n## **6. Monitoring and Performance**\n\nOnce deployed, don’t forget to monitor your API.\n\n### ✅ **Logging**\nUse services like **Logflare**, **Papertrail**, or **Render’s built-in logs**.\n\n### ✅ **Uptime Monitoring**\nUse **UptimeRobot** or **Pingdom** to check if your"
}